# Shoresh — Platform State

_Last updated: 2026-09-04 (**Projection failures on the sync path are now detected and recoverable — schema v55.** Previously, if a device received a replicated op over LAN sync whose op-log insert succeeded but whose projection into a local table then threw (`applyRemoteOp` in `electron/sync/syncClient.js`), the failure was only `console.error`-logged — the device's materialized state silently and permanently diverged from its own op-log, with no way to detect or fix it short of re-syncing from scratch. A new host-local, never-synced `projection_failures` ledger (schema v55) now records every such failure keyed by `op_id`; `electron/ops/projectionRepair.js` provides entity-scoped repair (`repairProjectionForEntity`) that replays exactly that entity's ops in seq order to reconstruct its row, and a read-side `checkProjectionHealth` check. Repair fires automatically on both devices after a short debounce once sync traffic quiets down (`electron/sync/syncClient.js`), and is otherwise reachable only through two new MCP support-surface tools, `check_projection_health` (always available, read-only) and `repair_projection_entity` (gated behind `--allow-write`, per `scripts/mcp/server.js`) — there is no renderer IPC and no director-facing UI for this in v1. See ADR `docs/adr/2026-09-04-projection-failure-detection-and-recovery.md`, the Database Tables and Edge Functions/API Routes sections below. Prior header note follows.)_

_Prior: 2026-09-03 (**Ingest asks the director to interpret compound schedule cells (T118) — schema v54.** A new pure classifier (`src/ingest/compoundCellPatterns.js`) detects cells like "Lunch + Leave" or "Change/Snack" that a naive importer previously read as their own standalone activity — silently fabricating a scheduling rule for something that isn't real (verified end-to-end: one real camp file produced 2,233 spurious UNDERSERVED findings this way). Detection is structural (a word that pairs with ≥2 distinct partners is a wrapper/rotation candidate; a word that always pairs with the same partner is one fixed name, e.g. "Arts & Crafts") and never auto-splits or auto-merges. A new "Cells We Weren't Sure About" section on `ImportScreen.jsx` (same visual family as the existing "Longer Blocks" cards, nothing pre-selected) asks the director once per distinct pattern — one thing as written / a wrapper around [anchor] / co-equal alternatives / not sure yet — and a confirmed answer is written to a new host-local, never-synced `compound_cell_decisions` table (schema v54, `electron/ops/confirmCompoundCellPattern.js`) so a later import at the same camp never asks again. See ADR `docs/adr/2026-09-03-compound-cell-interpretation.md` and "What's changed since" below. Prior header note follows.)_

_Prior: 2026-09-02 (**Imported schedule now saves as a pull-up-able version (T117) + typo-dedup sweep completed — no schema change (still v53).** Two small correctness/completeness arcs landed since the 2026-08-31 note. (1) **Import materializes a saved version** (ADR `docs/adr/2026-09-02-imported-schedule-materializes-as-a-version.md`, T117 slices 1–2): bringing in a prior year's schedule via the Import screen now captures the grid's actual (group × day × block → activity) placements and, immediately after the catalog commit, writes them as ONE new `schedule_snapshots` row named "Imported schedule" on the camp's manual-route schedule — pull-up-able from the Schedule screen's version history the same as any hand-saved version, but never auto-filling the live working grid ("grid = workbench, versions = woodpile"). A placement that can't be matched against the just-imported catalog is reported to the director as skipped, not silently dropped; re-importing (Add or Replace) always adds another version rather than overwriting the last one. A related fix closes a gap this surfaced: restoring ANY saved version now checks each cell against the CURRENT catalog first and non-destructively skips (with an on-screen "N cell(s)...were skipped" notice) any cell whose group/day/time-block/activity/anchor id no longer exists — previously a Replace re-import (which mints new catalog ids) could make restoring an older version silently write a broken grid. (2) **Typo-insensitive duplicate detection, sweep completed**: every remaining place in the app where a director types a name and the app offers to create a new entry now recognizes an existing one that differs only by case or extra whitespace — closing the last gaps (the schedule grid's inline cell editor, Activities screen add/copy/quick-add/bulk-paste, Electives, Special Events) in a sweep that started earlier. See "What's changed since" for detail. Prior header note follows.)_

_Prior: 2026-08-31 (**Lifecycle-IA nav redesign + Roots home as a distinct screen + Special Events unification + schedule-screen (WS5) rebuild — schema v48–v53, PRs #209–#242.** Five arcs landed since the 2026-08-27 note. (1) **Stage-aware navigation + landing** (ADR `docs/adr/2026-08-28-stage-aware-nav-landing.md`, WS1, PRs #211/#212): the sidebar's old "Camp Set Up"/"Schedule" two-section model is replaced by a fixed, chevron-less **Roots** row plus three collapsible lifecycle stages — **Germination** (Age Divisions/Groups/Days/Time Blocks/Locations) / **Sprouts** (Activities/Fixed Events/Recurring Events/Electives/Special Events) / **Plants** (the four schedule-building destinations, now pinned so they never collapse out of view). A brand-new camp with no setup data lands on a new **`SeedScreen`** (`seed` key) instead of Roots; every other camp still lands on Roots. Camp/Re-import/Conflicts/Trash moved off the top-level sidebar into a gear-button admin menu; Devices stays a separate admin-only entry. (2) **Fixed vs Recurring events un-conflation** (ADR `docs/adr/2026-08-28-fixed-vs-recurring-events.md`, WS2, schema v51): `anchor_activities` gained a `kind` CHECK column (`'fixed'|'recurring'`, all-camp vs. group/division-scoped), and the sidebar now shows **two** rows — "Fixed Events" and "Recurring Events" — both routing to the same `AnchorsScreen` filtered by a fixed `kind` prop (`ANCHOR_KIND_BY_SCREEN` in `App.jsx`), not two screens. (3) **Roots home is now a distinct screen** (ADR `docs/adr/2026-08-28-roots-home-is-a-distinct-screen.md`, WS4, PRs #214/#215/#217/#218/#219/#220): `RootsHomeScreen.jsx` **replaces** the former `roots` route (which was `ReconciliationScreen` mounted in a fixed `mode="inspect"` prop) — no more census/diff vocabulary, no RootMap/RootMapPanel/RootsBanner on this screen. It shows a bento of live structure counts (`useCurrentStructureCounts`) plus a "Needs your attention" list unioning **persisted, cross-session open reconciliation decisions** (new host-scoped `open_reconciliation_decisions` table, schema v52, ADR `docs/adr/2026-08-28-persisted-reconciliation-decisions.md`) with live structure checks. `ReconciliationScreen`'s census/RootMap machinery is now scoped *only* to the actual import-a-file reconcile flow (`ImportScreen.jsx`'s `mode="import"`), per the ADR's "census stays import-only" ruling. (4) **Special Events unification** (ADR `docs/adr/2026-08-29-unify-special-events-screen.md`, PRs #235/#242): the separate `EventScreen`/`SpecialDaysScreen` authoring rows are replaced by one `SpecialEventsScreen.jsx` (`specialevents` key, labeled "Special Events") — a single type-tagged, inline-add table over `special_days` + `events`; the Plants-side build surfaces (`SpecialSchedulesScreen`, `schedule:special`) are unchanged. (5) **Schedule screens rebuilt (WS5, PRs #221–#229/#236–#238)**: the activity palette is now a filterable **Ledger** (`PaletteLedger` + a text filter, replacing the old flat palette); the toolbar is slimmed; **Daily View is the default** (`useState('day')`, was Group View); double-click on a grid cell opens an inline editor (`CellInlineEditor`) instead of a separate `EditModal`; an overflow popup was replaced by a quiet visible cluster; stat badges hide when their target is unconfigured. The dead `template_overlays`/`schedule_snapshots.overlays` pair was **dropped at schema v53** (ADR `docs/adr/2026-08-30-retire-overlay-stamp-subsystem.md`) — no live writer had existed since the day-overrides re-point (T108); events/electives/day_overrides are now the whole "special things on the schedule" family. Several whole-app "coherence wave" design passes (PRs #230–#234/#238) consolidated the colored-pill chip shape and the caution/error banner styling into shared `src/styles/shared.js` tokens and added a shared `InlineAddRow` setup-screen primitive (PR #241, used by `SpecialEventsScreen` and others) and a shared `ScheduleDoor` component. A `graphify` codebase knowledge graph was also wired into the repo (PRs #226/#227, tooling only, not a product-facing change). See the Navigation Model, Screens, Database Tables, Components, and Home Screen Layout sections below, and "What's changed since" for arc-by-arc detail. Prior header note follows.)_

_Prior: 2026-08-27 (**Spatial layer removed + Roots hub redesign — PRs #201/#204/#205/#206.** Four arcs landed since the 2026-08-25 note. (1) **Spatial/map layer fully removed** (PR #201, commit `335e311` — a `chore:` removal, no ADR): the entire "run the day on the map" surface — the **Day Map** screen (`schedule:map`, `DayMapScreen.jsx`), the Locations Map tab + drag-to-place Canvas, the Tile-World simulation, `src/data/deriveOccupancy.js`, and every map helper — was **deleted**; `LocationsScreen` is now **list-only** (name/capacity/notes CRUD). Owner rationale: "it didn't fit our infrastructure." The v50 map columns (`camp_maps`, `locations.map_id`/`map_geometry`/`kind`/`grid_x`/`grid_y`) are **kept dormant in the schema as a rebuild seam** — a fresh Phaser-based schedule-driven map is a separate future initiative. (2) **Roots hub — Bento layout** (PR #204): the census/domain grid became a responsive Bento (CSS `auto-fit` grid, weight-follows-attention emphasis). (3) **Sort-order retirement** (PR #205): the manual Sort Order input is retired; order is derived. (4) **Roots hub — census tiles are the interface** (PR #206, ADR `docs/adr/2026-08-27-roots-hub-tiles-are-interface.md`): the four census tiles (Understood / Needs attention / Changed / Not in source) are now the permanent top-level surface driving one focused panel; the Bento domain grid is **demoted to the Understood tile's drill-down view only** (no longer an always-on backdrop). The `RootsBanner` was trimmed to two actions — **"Re-import last year"** + "Download worksheet" (the dead "Facility map" button, which pointed at the removed spatial layer, is gone). See the Screens / Loc-Facility / Removed-Replaced sections. Note: schema-version numbers below were accurate as of the 2026-08-25 spatial arc and were not re-verified in this doc pass. Prior header note follows.)_

_Prior: 2026-08-25 (**Unified schedule-overlay program + Roots dashboard restructure + Day Map — schema v39–v47.** Three arcs landed since the last update. (1) **Unified schedule-overlay model** (ADR `docs/adr/2026-08-23-unified-schedule-overlay-model.md` + `2026-08-23-activity-recurrence-tiers-ingestion.md`): the app's five "special things on the schedule" — Recurring Events (`anchor_activities`), Electives (`elective_sets`), Events (`events`), Special Days (`special_days`), field-trip overlays (`template_overlays`) — are treated as ONE family along a **contend-and-coexist vs override-and-replace** seam. Concretely shipped: a first-class **`events` overlay entity** with its own internal sub-schedule (schema v40/v41, `EventScreen`); **"Fixed Events" renamed "Recurring Events"** at the UI with a daily/weekly recurrence axis on anchors (v42); **elective binding vectors** + per-offering `camper_headcount` capacity (v39/v43); an **activity truth-status ontology** column `activities.recurrence_truth_status` (asserted/obligation/permission, v44) written create-only by the ingest classifier; **engine location-contention** — anchors/events/electives now register their location in the engine's capacity map so a booked room reads as full and the engine routes around it (v45 `location_id` columns + `LocationPicker`); a **two-rows split suggestion** for multi-pattern activities with host-local decline-memory (`declined_two_row_splits`, v47); and **merged-cell ingest** (multi-block spans read from XLSX `!merges`). The dead `day_override_templates` pair was dropped (v46). (2) **Roots dashboard restructure** (PR #181): the **Context domain was removed** (5 census domains → 4: Structure / Scheduling / Time / Facility) and Events/Special Days/Electives folded into the **Scheduling** box. (3) **Locations "run the day on the map"** (PRs #182/#183): create-a-location-in-place on the camp map, and a new read-only **Day Map** view (`schedule:map`, `DayMapScreen`) showing each group at its activity's room per block with over-capacity rooms lit as traffic jams (ADR `docs/adr/2026-08-24-run-the-day-on-the-map.md`). See the Screens/Database Tables/"What's changed" sections below. Prior header note follows.)_

_Prior: 2026-08-21 (**Electives / special-days / day-overrides audit program — schema v36/v37/v38, PR #132.** The architecture/product audit of the three "point-of-intent" capabilities (`docs/work/specs/2026-08-20-electives-specialdays-facility-audit.md`) produced four ratified ADRs and three implemented areas: (1) **electives authoring** — schema v36 added `elective_sets.is_reusable` (the durability marker: reusable set vs one-off), plus inline point-of-intent authoring from an empty schedule cell (T110); (2) **special-days author UI** — schema v37 added `special_days.notes`, and `SpecialDaysScreen.jsx` (+ `specialDay/SpecialDayGridEditor.jsx`/`SpecialDayCell.jsx`) is now a reachable standalone author screen (`specialdays` nav key, T106); (3) **day-overrides re-point** — schema v38 REPLACED the old `day_override_templates` model with a route-agnostic, engine-blind, render-time `day_overrides` diff table (swap/pull authored in-grid via ScheduleScreen "override mode") + `schedule_snapshots.day_overrides_json` so a whole-week snapshot restore carries overrides (T108). The old `DayOverridesScreen.jsx` and the `dayoverrides` nav key are RETIRED. Follow-ups still open: T107 (special-days → Roots Context), T109 (orphaned span-tail guard), T113 (override undo/redo), and the ratified-but-unbuilt facility-topology foundation. See the Database Tables entries + "What's changed" below. Prior header note follows.)_

_Prior: 2026-08-21 (**New entity families — schema v34/v35.** T40 slice 1 landed `special_days`/`special_day_time_blocks`/`special_day_slots` (v34) — a standalone one-day special-event schedule; T41 slice 1 landed `elective_sets`/`elective_set_activities` + `template_slots.elective_set_id` (v35) — group-level electives, with the engine skipping elective cells. BOTH were **data-shape slices only** at the time — the author UI/render layers landed in the v36–v38 program above. Also: an **ingestion CLI** (`scripts/ingest.js`, T51) and the **T101** location rename-recollide mitigation shipped.)_

_Prior: 2026-08-20 (**Roots is now the app home/dashboard** — PR #113/#114. The `roots` route is the landing screen; `ReadinessHub` is retired and its verdict moved onto the Roots banner (single-source `getReadiness`); RootMap nodes are primary "Manage {Area} →" navigation; the banner carries Import/Worksheet/Facility entry points; a finished import routes back to Roots with a post-import banner + grace-window undo; setup screens gained a "← Roots" return. Design: `docs/work/specs/2026-08-19-roots-dashboard-spine-design.md`; plan: `docs/work/plans/2026-08-19-roots-dashboard-spine.md`. No schema change (still v33). Prior deepening — quiet-at-first-glance default, per-entity census roster, persistent read-only Roots inspector, Context wired to the authored special-schedule layer — remains as documented in ADR `docs/adr/2026-08-19-roots-census-and-persistent-inspector.md`.)_

---

## Stack

| Layer | Technology |
|---|---|
| Frontend | React 19 + Vite, plain inline-style objects (no CSS files/modules) |
| Desktop shell | Electron 43 (`electron/main.js`), contextBridge IPC via `electron/preload.js` |
| Local database | SQLite via `better-sqlite3`, one file per device, schema versioned in-app (`electron/db/schema.sql` + migration blocks in `electron/db/localDb.js`) |
| LAN sync | Custom `ws`-based protocol: one device runs a WebSocket host (`electron/sync/syncServer.js`), others connect as clients (`electron/sync/syncClient.js`); mDNS discovery via `bonjour-service` (`electron/sync/discovery.js`) |
| Legacy backend (fully retired) | `@supabase/supabase-js` is **no longer a dependency** (removed from `package.json`/`package-lock.json`, 2026-07-24). `src/supabase.js` and `supabase/migrations/` have moved to `legacy/supabase/` (with `legacy/supabase/README.md` explaining why they're kept). Zero active imports of Supabase remain anywhere under `src/` or `electron/` — confirmed by a fresh inventory re-run, and enforced going forward by an ESLint `no-restricted-imports` rule (`eslint.config.js`) banning any `@supabase/*` import under `src/**`/`electron/**`, proven to actually fire by `eslint.supabase-ban.test.js`. `App.jsx`'s `seedDays()` (the last live call site) was ported to `localClient`/local `days_of_operation` — see `src/utils/seedDays.js` — before this cleanup landed. |
| Repo | git@github.com:gfeitel1-maker/Scheduling-Project-.git (local clone at `Scheduling-Project-/`) |

**Architecture note:** the app's migration off Supabase (Postgres + Auth + RLS) to the local-first Electron/SQLite/LAN-sync design is **fully complete**, including retirement of the legacy backend itself (not just every screen). Each device has its own SQLite db; one device acts as a LAN "Host" (WebSocket server), others are "Clients" that sync over the LAN. Data isolation that used to be enforced by Postgres RLS is now enforced by the app being fundamentally single-camp-per-device-db (see Database Tables below) plus signed session tokens. New engineering work should target the Electron/SQLite path — `legacy/supabase/` is historical reference only, and the ESLint rule above blocks reintroducing it. The previously-CRITICAL `bulk_replace` cross-device seq bug (see Key Architectural Decisions) is now **fixed** — the Schedule screen's Regenerate flow no longer spuriously conflicts.

---

## Navigation Model

Two nested state machines, no router:

1. **Device/session phase** — `src/hooks/useDeviceMode.js` derives a `phase` from local state (`error` → `loading` → `mode-select` → `bootstrap`/`join` → `pairing_pending` → `pairing_denied` → `login` → `session`). `src/App.jsx`'s top-level `App()` switches on `device.phase` to render one of: `ModeSelectScreen`, `CampBootstrapScreen` (Host: create a new camp), `JoinScreen` (Client: pick a discovered Host), `PairingPendingScreen` (Client: waiting for admin approval), `LoginScreen`, or the full `AppShell`. `pairing_denied` renders an inline error state in `PairingPendingScreen`.
2. **In-app screen** — once in a session, `AppShell` (`src/App.jsx`) holds a `screen` string in `useState`, looked up in the `SCREENS` map and passed down through `Shell` → `Sidebar` (`src/components/layout/`). **Default landing screen is stage-aware** (ADR `docs/adr/2026-08-28-stage-aware-nav-landing.md`): `useState(() => campIsEmpty ? 'seed' : 'roots')` — a brand-new camp with no setup data lands on `SeedScreen` (`seed` key, the "Seed your camp" first-run screen, never reachable again once `campHasSetupData()` is true), every other camp lands on `RootsHomeScreen`. `campId` and an `onNavigate` (`navigate`) callback are threaded as props into every screen — no context, no router. The retired `'readiness'` key is **redirected to `'roots'`** at render (`resolvedScreen = screen === 'readiness' ? 'roots' : screen`) so stale deep-links/nav targets still resolve. The `navigate()` wrapper also carries drill-in focus targets (an elective set id, an event/special-day id) between screens, clearing each one the moment the director leaves the screen it targets.

**Sidebar structure** (`src/components/layout/navSections.js`, ADR `docs/adr/2026-08-28-stage-aware-nav-landing.md` Decision 3) — the "Seed → Germination → Sprouts → Roots → Plants" lifecycle IA (`docs/work/specs/2026-08-28-lifecycle-ia-program.md`): a fixed, chevron-less **Roots** row at top (`ROOTS_ITEM`, no fold state, always reachable); three collapsible, badge-counted stages (`NAV_SECTIONS`) — **Germination** (Age Divisions, Groups, Days, Time Blocks, Locations — the irreducible structure), **Sprouts** (Activities, Fixed Events, Recurring Events, Electives, Special Events — the program that grows from it), and **Plants** (Generated Schedule, Manual Build, Special Schedules, Elective Schedules — the schedules themselves; **pinned**, never collapsible, so the active-route highlight stays visible); and a gear-button **admin menu** (`ADMIN_MENU_ITEMS`: Camp, Re-import last year, Conflicts (badge), Trash) with `ADMIN_ONLY_MENU_ITEMS` (Devices, labeled "LAN & Devices") appended for `role === 'admin'`. Camp Setup's former standalone "Camp Setup" screen is gone — its one remaining setting lives under the gear's "Camp" item.

All Electron/SQLite calls from the renderer go through `window.shoresh.*` (see `electron/preload.js`), backed by IPC handlers in `electron/main.js`: `chooseMode`, `discoverHosts`, `login`, `createUser`, `bootstrapCamp`, `write`, `bulkReplace` (new, see below), `verifySession`, `getCamp`, `listUsers`, `list`, `getDeviceId`, `resolveConflict`, `listPendingConflicts`, plus push events `onOpApplied`/`onOpConflict`. `src/localClient.js` wraps every one of these (`write`, `bulkReplace`, `deleteEntity`, `list`, etc.) — screens must always go through `localClient`, never call `window.shoresh` directly.

**`bulkReplace` IPC surface (new, added during Sub-plan E Task 3):** `shoresh:bulk-replace` — admin-gated (mirrors `write`'s auth shape plus a `role === 'admin'` check, since a bulk_replace is a wholesale delete+reinsert of a scope, more destructive than any ordinary field write). Routes to the already-built `bulk_replace` op-log primitive (`appendBulkReplaceOp`/`applyBulkReplaceProjection` in `electron/ops/operations.js`, built in Sub-plan A Task 3). `BULK_REPLACE_ENTITIES` registers `template_slots` (columns: `id, template_id, group_id, activity_id, day_id, time_block_id, anchor_id, is_anchor, is_span_head, flags`), scoped by `template_id`. (`template_overlays` was the second registered entity here until it was retired at schema v53 — see Database Tables.)

---

## Auth

Local, PIN-based, per-camp — not Supabase Auth (see architecture note above).

### Token types (device trust model)

Two token types are minted, verified, and enforced separately — see `electron/auth/localAuth.js` and `docs/adr/2026-07-25-device-trust-revocation.md`.

**Camp token (`type: 'camp'`)** — issued exclusively by the Host using its Ed25519 private key (`host_signing_key` table, generated once at `bootstrapCamp()`). Payload: `{type, userId, deviceId, campId, iat, exp, jti}`, base64url-encoded JSON, signature appended as `{payload}.{sig}`. 24h TTL (`TOKEN_TTL_MS`). The Host's Ed25519 public key is stored as `camps.signing_public_key` (hex-encoded DER/SPKI) and replicated to Clients via full-sync — Clients can verify camp tokens but can never mint them (private key never leaves the Host). `syncServer.js`'s `handleAuthenticate` rejects any `local` token on the network path outright.

**Local token (`type: 'local'`)** — issued by the Client itself using its device-scoped HMAC-SHA256 secret (`devices.device_secret_identifier`, a 32-byte hex value minted by the Host at device pairing approval). Used only for offline Client sessions (local IPC path). Never accepted on the WS authentication path.

**Which token is issued** is re-derived from `host_signing_key`'s presence in the DB (`issueTokenForThisDevice`), never passed in by the caller — so callers can't get the routing wrong.

**Token verification** (`verifySessionToken(db, token)`): the `type` claim dispatches to Ed25519 verification (camp) or HMAC-SHA256 (local) only after parsing the payload — a tampered type claim doesn't reroute; it fails signature verification. `exp` is enforced (tokens past their TTL return null). Malformed tokens, unknown types, and missing DB rows all fail closed (return null).

**Renewal**: `renew_token` WS message — Host re-checks revocation status before issuing a fresh camp token; client schedules renewal at ~20h.

### Device pairing and revocation

- New Client sends a `pairing_request` WebSocket message; Host records it as a pending device.
- Admin approves in `DeviceManagerScreen.jsx` — Host mints a `device_secret_identifier` and sets `devices.authorized_at`.
- Revocation: admin revokes via Device Manager → Host sets `devices.revoked_at`, `revoked_by_user_id`, `revocation_reason`; live WS socket for the revoked device is closed immediately.
- `devices` table fields: `id, name, last_seen_at, last_synced_at, last_synced_seq, authorized_at, revoked_at, revoked_by_user_id, revocation_reason, device_secret_identifier`.
- `authorize()` checks device revocation status on every privileged call — a revocation takes effect on the very next call from an already-connected device.
- `useDeviceMode.js` pairing phases: `pairing_pending` (request sent, awaiting admin approval) and `pairing_denied` (request rejected).

### Login paths

- `electron/auth/localAuth.js`: `attemptLogin(db, {name, pin, deviceId})` does the PIN check (`scryptSync` against `pin_hash`/`pin_salt`) + lockout tracking (`LOGIN_MAX_ATTEMPTS = 5`, `LOGIN_LOCKOUT_MS = 30s`). Issues the appropriate token via `issueTokenForThisDevice`.
- **Two login paths**, unified through `attemptLogin` so behavior can't drift:
  - Local IPC `login` handler (`electron/main.js`) — Host's own login, and a Client's offline fallback.
  - Unauthenticated WebSocket `login` message (`electron/sync/syncServer.js`) — lets a genuinely fresh Client verify its PIN against the Host and receive a camp token before its first sync.
- `useDeviceMode` derives `phase: 'login'` when there's a camp/host but no token yet; `phase: 'session'` once a token exists.

### Role-Based Behavior — centralized authorization layer (Phase 2, 2026-07-25)

**Every** mutating IPC handler (`electron/main.js`) and every mutating WebSocket handler (`electron/sync/syncServer.js`) now calls `authorize({db, token, action, resourceId})` (`electron/auth/authorize.js`) before proceeding — role is re-derived fresh from the `users` table on every single call, never trusted from the token payload or cached on an open connection. This replaces the old state (four scattered inline `role !== 'admin'` checks, only user-creation confirmed gated, the WS layer entirely unchecked) with one central, unit-tested, default-deny primitive.

- **`authorize()`** (`electron/auth/authorize.js`): verifies the token (`verifySessionToken`), re-queries `users`/`devices` by the decoded ids, resolves current role, looks it up against the permission matrix. Never throws (a db-layer failure returns `{allowed:false, reason:'db_error'}`, a malformed `action` is rejected for every role including admin). Returns `{allowed:true, userId, deviceId, role}` or `{allowed:false, reason}`.
- **Permission matrix** (`electron/auth/permissions.js`): `PERMISSIONS.admin = ['*']`; `PERMISSIONS.staff` is an explicit array — `<entity>.read`/`<entity>.write` for every entity in the exported `ENTITIES` list (13 entities), plus `users.read`, `devices.read`, `conflicts.read`, `conflicts.resolve`. Staff does **not** get `<entity>.delete`, `<entity>.bulk_replace`, `camps.rename`, or `users.create` — admin-only, matching pre-existing behavior exactly (no regression, enforcement only).
- **Shared action-derivation** (`electron/auth/deriveWriteAction.js`): `deriveWriteAction({entity, field})` (3-way: `DELETE_FIELD` sentinel → `<entity>.delete`, `camps`+`name` → `camps.rename`, else → `<entity>.write`) and `deriveBulkReplaceAction(entity)` (→ `<entity>.bulk_replace`) are called identically by both `electron/main.js` (IPC: `write`, `bulk-replace`) and `electron/sync/syncServer.js` (WS: `submit_op`, `acquire_lock`, `submit_bulk_replace_op`) — a staff user cannot do via a direct WebSocket connection anything they're blocked from via the renderer/IPC path, or vice versa.
- **WS identity**: `authorize()` calls on the WS path use `ws.token`, set exactly once during the `authenticate` handshake from the already-verified token — never a client-claimed field inside a later message body (`msg.op.device_id` etc. are also server-overwritten with `ws.deviceId`, unchanged from before this phase).
- **Deliberately left unwrapped** (no session token available at these call sites — pre-login/handshake-establishing, not an oversight): IPC `login`, `verify-session`, `choose-mode`, `discover-hosts`, `bootstrap-camp`, `get-camp`; WS `authenticate`, `login`. `get-camp`'s query was narrowed to `SELECT id, name` (was `SELECT *`) during Phase 2 review — it had been unknowingly leaking `camps.signing_secret` (the HMAC key backing every session token) to any unauthenticated caller, which combined with staff-readable `listUsers`/`getDeviceId` would have let a compromised renderer script forge an admin token. Fixed before Phase 2 Task 2 was accepted; no renderer code depended on the dropped column.

| Feature | admin | staff |
|---|---|---|
| Camp setup, tiers, groups, time blocks, activities, anchors, day overrides, cohorts, templates — ordinary read/write | ✅ | ✅ (server-enforced via `<entity>.write`/`<entity>.read`, not just client-side button-hiding) |
| Delete any entity, bulk-replace (schedule regeneration/snapshot restore), rename camp | ✅ | ❌ (server-enforced, `<entity>.delete`/`<entity>.bulk_replace`/`camps.rename` absent from staff's matrix) |
| Create users (`createUser` IPC) | ✅ | ❌ (`users.create`, admin-only — same behavior as before Phase 2, now routed through `authorize()` instead of an inline check) |
| Schedule view/edit, conflict resolution | ✅ | ✅ |

`users.role` is a `CHECK (role IN ('admin', 'staff'))` column, re-queried fresh by `authorize()` on every privileged call — a role change (or a user/device deletion) takes effect on the very next call using an already-issued token, proven by a same-token role-flip test on both the IPC and WS paths (`electron/main.test.js`, `electron/sync/syncServer.test.js`).

---

## Screens

| Screen key | File | Notes |
|---|---|---|
| (mode-select, not in `SCREENS` map) | `src/screens/ModeSelectScreen.jsx` | Choose "Host a camp" vs "Join a camp" — pre-session |
| (bootstrap) | `src/screens/CampBootstrapScreen.jsx` | Host: create a new camp + admin user — pre-session |
| (join) | `src/screens/JoinScreen.jsx` | Client: pick a discovered Host from mDNS results — pre-session |
| (pairing_pending / pairing_denied) | `src/screens/PairingPendingScreen.jsx` | Client: waiting UX after sending a pairing request; also renders the denied state inline — pre-session |
| (login) | `src/screens/LoginScreen.jsx` | Name + PIN entry — pre-session |
| ~~`readiness`~~ | ~~`src/screens/ReadinessHub.jsx`~~ | **RETIRED (plan T3, PR #113).** `ReadinessHub.jsx` is deleted; the six-state readiness verdict no longer has its own screen or banner (see the `roots` row below — the current `RootsHomeScreen` deliberately carries no verdict banner). A stale `'readiness'` nav target/deep-link is redirected to `'roots'` at render. |
| `seed` | `src/screens/SeedScreen.jsx` | **NEW (WS1, ADR `docs/adr/2026-08-28-stage-aware-nav-landing.md` Decision 1).** The first-run landing for a camp with no setup data yet — never reachable again once `campHasSetupData()` is true. Carries the prominent "bring data in" entry point (Import last year) for a brand-new camp; a populated camp instead reaches import via the gear menu's "Re-import last year". |
| `roots` | `src/screens/RootsHomeScreen.jsx` | **The app HOME/dashboard — REPLACED the former `ReconciliationScreen`-in-`mode="inspect"` roots screen (ADR `docs/adr/2026-08-28-roots-home-is-a-distinct-screen.md`, WS4).** Default landing screen for any camp with setup data (`campIsEmpty ? 'seed' : 'roots'`). Deliberately carries **no census/diff vocabulary and no RootMap/RootMapPanel/RootsBanner** — those stay scoped to the actual import-a-file reconcile flow. Two panels: (1) a **bento of live structure counts** (`useCurrentStructureCounts`, name-chips per card — Activities/Groups/Age Divisions/Locations/Days & Blocks/Recurring Events) reading the camp's current tables directly, not an import diff; (2) a **"Needs your attention" list** (`buildAttentionList`/`buildStructureIssues`, `src/ingest/attentionList.js`) unioning **persisted cross-session open reconciliation decisions** (`useOpenReconciliationDecisions`, backed by the new `open_reconciliation_decisions` table, schema v52 — see Database Tables) with a minimal set of live structure checks. Also carries a `ScheduleDoor` (plain, non-verdict door to Plants) and a worksheet download action. The six-state readiness verdict banner from the old `ReadinessHub`/`RootsBanner` design was deliberately cut (Governor brief decision, WS4) — Schedule is presented as a plain door, never a verdict. |
| `camp` | `src/screens/CampScreen.jsx` | Camp-level setup entity screen |
| `import` | `src/screens/ImportScreen.jsx` (upload/parse antechamber) + `src/screens/ReconciliationScreen.jsx` (R2'b, one continuous reconciliation surface; as of ADR `docs/adr/2026-08-28-roots-home-is-a-distinct-screen.md`, its census/RootMap machinery is scoped exclusively to this import-a-file flow — it is no longer reused for the `roots` screen, which is `RootsHomeScreen`, see above) | Onboarding/reconciliation: ingest a prior-year schedule or workbook, then reconcile it against the camp's current setup in one screen — a header "N of Y done" spine, an understood receipt (collapsed, never counted as work), and a **Roots root-map projection** (`RootMap` illustration + `RootMapPanel`) over the SAME report: navigate by **condition** (state tiles: Understood/Needs attention/Changed/Not in source) OR by **domain** (root/child nodes: Structure/Scheduling/Time/Facility), both projections of one `buildReconciliationReport` model (ADR `2026-08-19-roots-census-and-persistent-inspector.md` + `2026-08-18-rootmap-screen-port.md`). The default (no selection) view scopes to the **unresolved** needs-attention subset — quiet at first glance, with a "N resolved · Show all" reveal — while a node selection opens that area's per-entity census roster (deep on demand). Decision cards carry progressive-disclosure evidence, a not-in-source gap, and a sticky staged tray whose two buttons ("Use this setup" / "Apply confirmed changes and keep the rest for review") are the first real (non-dry-run) commit calls — everything before that is `ingestReconcile` dry runs re-run on every triage action. Reviews include an editable **unit column** per group, **fixed-event routing** (a pinned event like Lunch defaults out of the activity catalog with a one-click keep-it override), a **"Cells We Weren't Sure About" section** (T118, ADR `docs/adr/2026-09-03-compound-cell-interpretation.md`) that asks the director once per distinct compound cell pattern (e.g. "Lunch + Leave") whether it's one thing as written, a wrapper around a real activity, or co-equal alternatives — remembered per camp so a later import never re-asks — and hold-lane cards for genuine identity/staleness conflicts (folded into the same card vocabulary, not a separate takeover modal). Director hand-edits are stamped human provenance so a re-import can't silently overwrite them. `ReconciliationQueue.jsx`, `ReconciliationSummary.jsx`, `ReconciliationLedger.jsx`, and the held-conflict takeover modal are deleted. The v1 cutover dropped two smaller affordances not yet carried over: the "Remember this" alias-confirmation checkbox and the held-identity "skip this row" option — both are a plain resolution, just without those two conveniences. Grace-window undo is explicitly deferred to its own later slice (not shipped). |
| `trash` | `src/screens/TrashScreen.jsx` | Restore deleted records from the op-log |
| `tiers` | `src/screens/TiersScreen.jsx` | |
| `groups` | `src/screens/GroupsScreen.jsx` | |
| `days` | `src/screens/DaysScreen.jsx` | Days of Operation CRUD. Wired into `SCREENS`/`Sidebar` 2026-07-26 (previously existed but was unreachable — `TopBar` already reserved the `days` title). Rows auto-seeded Mon–Fri by `seedDays.js`; this screen edits them. |
| `timeblocks` | `src/screens/TimeBlocksScreen.jsx` | |
| `activities` | `src/screens/ActivitiesScreen.jsx` | |
| `anchors` / `fixedevents` | `src/screens/AnchorsScreen.jsx` | **Fixed vs Recurring un-conflation (WS2, ADR `docs/adr/2026-08-28-fixed-vs-recurring-events.md`, schema v51).** One screen, two sidebar rows/nav keys, each fixed to a `kind` prop (`ANCHOR_KIND_BY_SCREEN` in `App.jsx`: `fixedevents` → `'fixed'`, `anchors` → `'recurring'`) — not two screens. Code symbols stay `anchors`/`anchor_activities`; the table gained a `kind` CHECK column (`'fixed'|'recurring'` — all-camp vs. group/division-scoped). Both rows are `expected: true` in `navSections.js` (a "needs a look" affordance, not blocking). Anchors also carry `recurrence_level` (daily/weekly), an optional `schedule_week_id`, and an optional `location_id` (engine contention). |
| `cohorts` | `src/screens/CohortsScreen.jsx` | |
| `electives` | `src/screens/ElectivesScreen.jsx` | Build/manage elective sets and their offerings (setup side, Sprouts); add any activity to a set by import or inline create, not pick-from-catalog-only. Optional sidebar entry. Distinct from the Plants-side build entry `schedule:electives`/"Elective Schedules". |
| ~~`dayoverrides`~~ | ~~`src/screens/DayOverridesScreen.jsx`~~ | **RETIRED (T108, PR #132).** The standalone day-override editor and its `dayoverrides` nav key are deleted; day overrides are now authored **in-grid** on `ScheduleScreen` via "override mode" (route-agnostic, render-time diff — see `day_overrides` below). |
| ~~`events`~~ / ~~`specialdays`~~ | ~~`src/screens/EventScreen.jsx`~~ / ~~`src/screens/SpecialDaysScreen.jsx`~~ | **RETIRED — merged into `specialevents` (below), ADR `docs/adr/2026-08-29-unify-special-events-screen.md`, PR #235.** |
| `specialevents` | `src/screens/SpecialEventsScreen.jsx` | **Special Events unification (owner-approved 2026-08-29, PRs #235/#242).** Replaces the separate Events and Special Days authoring rows with one create/manage hub: a single table of `special_days` + `events` (type-tagged rows), **inline-add** (`InlineAddRow`, Wave C1/C capstone, PR #242) rather than a modal, click a row to open its detail (name/notes/location authoring, a `ScheduleDoor` link to "Build →"). Sub-detail editors carried over from the retired screens: `src/screens/event/EventGridEditor.jsx`/`EventCell.jsx` (an event's own internal sub-schedule) and `src/screens/specialDay/SpecialDayGridEditor.jsx`/`SpecialDayCell.jsx` (a special day's own time-grid × groups cells). The Plants-side build surface (`SpecialSchedulesScreen`, route `schedule:special`) is **unchanged** — only the Sprouts authoring surface merged. Optional sidebar entry. Not engine-touched (authored, never generated); an event/elective/day-override cell is still `template_slots.event_id`/`elective_set_id` or a `day_overrides` row, not a new cell kind. |
| `schedule` / `schedule:manual` / `schedule:generated` | `src/screens/ScheduleScreen.jsx` | Most complex screen — DnD schedule builder, **two coexisting ROUTES (Manual / Generated), each with its own candidate schedule**, three views each (group/day/activity, **Daily View is now the default** — WS5, was Group View). A `route` state keys `slots`/`snapshots`/`stats`/`findings`; the manual route's grid is `ManualBuildView`. Neither route is the "real" schedule and the app never picks one — Export asks, every time, and does not remember. Reads via `localClient.list`, single-slot writes via `writeFields`/`localClient.write`, bulk regen/snapshot-restore via `localClient.bulkReplace`, snapshot CRUD via `localClient.write` (the `overlays` JSON-text column was dropped at schema v53 along with `template_overlays` itself — see Database Tables). **WS5 rebuild (PRs #221–#229/#236–#238):** the activity palette is now a filterable **Ledger** (`src/components/schedule/ActivityPalette.jsx`'s `PaletteLedger` + a text filter, replacing the old flat palette); the toolbar is slimmed; double-click on a grid cell opens an inline editor (`CellInlineEditor.jsx`) rather than a separate modal; an overflow popup was replaced by a quiet visible cluster; stat badges (`StatBadge.jsx`) hide when their target is unconfigured; the Plants sidebar rows are pinned (never collapse). See Key Architectural Decisions for the CRITICAL sync bug this app's original Supabase migration verification surfaced. |
| `schedule:special` | `src/screens/SpecialSchedulesScreen.jsx` | **Schedule-side picker (NEW)** — pick/create a special-day schedule to build, opening `SpecialDaysScreen`'s grid editor. A picker, not a route (absent from `SCHEDULE_ROUTE_BY_SCREEN`); lives under the Schedule nav so "these builds live with the Schedule routes" (PR #169). |
| `schedule:electives` | `src/screens/ScheduleElectivesScreen.jsx` | **Schedule-side picker (NEW)** — sibling of `schedule:special`; pick an elective set to build its schedule, opening `ElectiveSetDetail` (PR #171). |
| ~~`schedule:map`~~ | ~~`src/screens/DayMapScreen.jsx`~~ | **RETIRED (PR #201, commit `335e311`).** The Day Map view and its `schedule:map` key are deleted along with the rest of the spatial layer; `DayMapScreen.jsx` and `src/data/deriveOccupancy.js` no longer exist. A future schedule-driven map is a separate (Phaser-based) initiative. |
| `conflicts` | `src/screens/ConflictsScreen.jsx` | Write-conflict resolution with pre-resolution DB snapshot; only screen given extra props (`onNavigate`, `pendingConflicts`) beyond `campId` |
| `devices` | `src/screens/DeviceManagerScreen.jsx` | Admin: approve pairing requests, view paired devices, revoke device access — admin-only |

---

## Components

- `src/components/layout/Shell.jsx`, `Sidebar.jsx`, `TopBar.jsx`, `navSections.js` — app chrome, the lifecycle-IA sidebar (Roots fixed row + Germination/Sprouts/Plants collapsible stages + gear admin menu, see Navigation Model), badge counts (e.g. pending conflicts)
- `src/components/CohortPicker.jsx` — cohort selection widget
- `src/components/ScheduleDoor.jsx`, `src/components/setup/InlineAddRow.jsx`, `SetupScreenShell.jsx` — shared cross-screen primitives from the whole-app design coherence passes (Waves 2/3, B/C, PRs #230–#234/#238/#241): a plain (non-verdict) door to a Plants build surface, and an inline blank-row "add" affordance used across setup screens (Wave C1) including `SpecialEventsScreen`. The colored-pill chip shape and caution/error banner styling were consolidated into shared style tokens (`src/styles/shared.js`) rather than new components.
- `src/components/schedule/` — schedule-builder-specific. Current set (WS5 rebuild, PRs #221–#229/#236–#238, replaced the pre-WS5 palette/modal set): `ActivityPalette.jsx` (now a filterable **Ledger** — `PaletteLedger` + text filter, replacing the old flat palette), `ManualBuildView`, `ScheduleActivityView`, `ScheduleDayView`, `ScheduleGroupView`, `SlotCell` (carries `data-overridden`, opens `CellInlineEditor.jsx` on double-click), `PulledCell` + `OverrideToggleButton` (day-override "override mode", T108), `CellInlineEditor.jsx` (double-click cell edit, replaced the old separate `EditModal`), `EmptyCell.jsx`, `ErrorBanner.jsx`, `ExclusionConfirmDialog.jsx`, `ExportChooserModal.jsx`, `FindingsRail.jsx`, `IndeterminateBar.jsx`, `ConfirmRegenModal.jsx`, `VersionsDropdown.jsx`, `StatBadge.jsx` (hides when its target is unconfigured), `WeekContextBar.jsx`, `WeekSwitcher.jsx`, `DeleteWeekDialog.jsx`, `ScheduleSkeleton.jsx`. The old `OverlayCell`, `EditModal`, `FieldTripDrawer`, and `FlagDetailModal` components no longer exist (superseded by the events/electives/day_overrides family and the WS5 rebuild).
- `src/components/reconciliation/` — scoped to the **import-a-file reconcile flow only** (`ReconciliationScreen`, `mode="import"`) as of the Roots-home-is-a-distinct-screen ADR; no longer doubles as the Roots home surface. `RootMap.jsx` (root-and-tree illustration over `src/assets/reconciliation/root-map.png`, node coords hand-placed in `rootMapLayout.js`), `RootMapPanel.jsx` (per-node panel — roster + primary "Manage {Area} →" navigation), `RosterList.jsx` (per-entity census roster, search + Groups-by-Age-Division grouping), `postImportBanner.jsx`, `ReconstructionMoment.jsx`, plus pure helpers `rootMapNav.js` (node → setup-screen routing, `DOMAIN_SCREEN`/`CHILD_SCREEN`), `domainRollup.js`, `reconciliationCards.jsx`. `rootsBanner.jsx` (the old dashboard-verdict banner) still exists in this directory but is no longer imported anywhere outside its own test — `RootsHomeScreen` does not use it (its bento/attention-list are built directly from `useCurrentStructureCounts`/`buildAttentionList`).

---

## Database Tables

(SQLite, one file per device — `electron/db/schema.sql`, schema **v55** as of this writing)

- **camps** — `id, name, signing_public_key` (Ed25519 public key, hex-encoded DER/SPKI — replicated to all Clients so they can verify camp tokens). Exactly one row expected per device db (single-camp-per-db assumption used throughout, e.g. `SELECT ... FROM camps LIMIT 1`).
- **host_signing_key** — `id (always 1), public_key, private_key, created_at`. Host-only table; generated once at `bootstrapCamp()`. Never replicated. The private key never leaves the Host device.
- **source_aliases** — `id, camp_id, entity_type, cohort_id, source_label, entity_id, status ('active'|'superseded'), confirmed_by, confirmed_at, superseded_by` (schema v30, S1b). **Host-local, never synced** — deliberately absent from every sync registry (`projections.js`/`campScopedEntities.js`/`syncServer.js`), the same pattern as `host_signing_key`, so a tablet Client can neither forge nor diverge alias identity. Durable memory of "this imported label (e.g. 'Reds') means this live entity (the Red group)", used by the import's `confirmed_alias` matching tier so a repeat import auto-resolves instead of re-asking. Written by exactly one committer, `electron/ops/confirmAlias.js` (host-only, admin-gated `source_aliases.confirm`, supersede-on-reconfirm); the generic op-log path in `operations.appendOp` refuses `entity === 'source_aliases'`. Populated from the director's "Remember this" checkbox on the import's ambiguous-identity resolution card.
- **import_evidence** — `id, camp_id, entity_type ('activities'|'anchor_activities'), entity_id, field, tag ('observed'|'inferred'), confidence ('high'|'low'), support (compact JSON), import_run_id, committed_at` (schema v31, B4). **Host-local, never synced** — deliberately absent from every sync registry (`PROJECTIONS`/`DIRECT_CAMP_ENTITIES`/`full_sync`), the same trust model as `source_aliases` and `host_signing_key`. Durable record of *why* an ingested field holds the value it does (which observations/inferences supported it), backing the reconciliation "why" disclosure. Written only on the host-only import commit path. See ADR `docs/adr/2026-08-10-ingestion-evidence-persistence.md`.
- **users** — `id, camp_id, name, pin_hash, pin_salt, role('admin'|'staff')`. Unique on `(camp_id, name)`.
- **devices** — `id, name, last_seen_at, last_synced_at, last_synced_seq, pairing_status ('pending'|'authorized'), authorized_at, authorized_by_user_id, revoked_at, revoked_by_user_id, revocation_reason, device_secret_identifier`. `last_synced_seq` is the op-log watermark for reconnect catch-up. `pairing_status` defaults to `'pending'`; flips to `'authorized'` on admin approval. `device_secret_identifier` is the HMAC secret for local tokens, minted at pairing approval. `revoked_at`/`revoked_by_user_id`/`revocation_reason` are set at revocation; live WS socket is closed immediately. `authorize()` checks `authorized_at IS NOT NULL` on every privileged call — a device row without it is always denied.
- **audit_events** — `id, camp_id, actor_user_id, device_id, action, outcome, reason, metadata (JSON), created_at`. Written by `electron/audit/auditLog.js`. Captured events include: `auth.login` (allow/deny with reason), `auth.authorize` (deny only — action + role + reason logged, no PIN/token material), device pairing and revocation events.`
- **operations** — the op-log: `seq (autoincrement), id (unique), entity, entity_id, field, value, author_user_id, device_id, timestamp, parent_op_id, client_write_id`. `client_write_id` is a client-generated idempotency key so a retried `submit_op` doesn't mint a duplicate op.
- **conflicts** — durable record of every detected write conflict: `id, entity, entity_id, field, incoming_op, existing_op, existing_op_id, created_at, resolved_at`. A conflict counts as resolved when any op has `parent_op_id = existing_op_id`.
- **locks** — `entity, entity_id, field, holder_device_id, acquired_at` — field-level edit locks.
- **groups**, **tiers**, **activities** — camp-scoped config entities, each `camp_id`-keyed.
- **template_slots** — `id, template_id, group_id, activity_id, day_id, time_block_id, anchor_id, is_anchor, is_span_head, flags`. `anchor_id`/`is_anchor` added in schema migration v17 (Sub-plan E Task 3) — were referenced by `BULK_REPLACE_ENTITIES`'s intended column set but physically missing from SQLite until this migration.
- **template_overlays — REMOVED (schema v53, ADR `docs/adr/2026-08-30-retire-overlay-stamp-subsystem.md`).** No live writer had existed since the day-overrides re-point (T108) retired its authoring path; `template_slots` (below) plus the events/electives/day_overrides family fully cover "special things on the schedule" now. `schedule_snapshots.overlays` was dropped in the same migration. Only registered `bulk_replace` entity today is `template_slots`. A rollback (`v53_down.js`) recreates the table (empty) for downgrade.
- **schedule_templates** — `id, camp_id, name, kind`. **A camp now holds up to TWO rows, one per schedule-building route** (`kind` = `'generated'` | `'manual'`), added by migration v23 (`CURRENT_SCHEMA_VERSION = 23`) together with `UNIQUE(camp_id, kind)` replacing v21's `UNIQUE(camp_id)`. Ids are derived, never minted: `deriveScheduleTemplateId(campId, kind)` (`electron/ops/scheduleTemplateId.js`) returns `schedule-template:${campId}` for the generated route — byte-identical to the pre-v23 one-argument form, which migration v21 still depends on — and `schedule-template:${campId}:manual` for the manual one. `kind` MUST be the first field written for a new row (write-ordering contract, documented at the `schedule_templates` entry in `electron/ops/projections.js`); written second, a manual row materialises as `'generated'`, collides, and is absorbed by `INSERT OR IGNORE`. Neither row is canonical — see `docs/adr/2026-07-28-plural-candidate-schedules-per-camp.md`. Legacy note: Local SQLite table (ported off Supabase in Sub-plan E Task 1/2). Was missing from `PROJECTIONS` (`electron/ops/projections.js`) until 2026-07-26 — ops were written to the op-log successfully but `applyProjection` silently no-op'd for every field, so the row never materialized. Fixed by adding the entry (commit `0ca1198`), same pattern as `groups`/`activities` etc. See Known Issues.
- **schedule_snapshots** — `id, template_id, name, is_auto, created_at, slots (TEXT/JSON), day_overrides_json (TEXT/JSON)`. **The `overlays` column was dropped at schema v53** alongside `template_overlays` itself (ADR `docs/adr/2026-08-30-retire-overlay-stamp-subsystem.md`); `slots` and `day_overrides_json` remain JSON-text columns — an explicitly scoped exception to this app's normal field-level op-log sync, not a pattern to reuse elsewhere (per the Sub-plan E design doc). Ported off Supabase in Sub-plan E Task 4. Gained a `PROJECTIONS` registry entry (`electron/ops/projections.js`) on 2026-07-24 — previously `ScheduleScreen.jsx`'s `writeFields()` calls to this entity silently never materialized the row (op-log write succeeded, but nothing projected it into the table); found and fixed while un-skipping the Sub-plan E Task 5 E2E regression test. **Second writer added 2026-09-02 (T117):** `electron/ops/materializeImportedVersion.js` writes one row named `"Imported schedule"` right after a schedule import commits (see "What's changed since"); `restoreSnapshot` (`src/screens/schedule/useSnapshots.js`) now drops any slot in a restored row whose referenced group/day/time-block/activity/anchor id no longer exists in the live catalog, reporting a skipped-cell count instead of writing a broken grid.
- **days_of_operation** — `id, camp_id, label, day_of_week, sort_order`. Default Monday-Friday rows are seeded on first camp mount by `src/utils/seedDays.js` (ported off Supabase 2026-07-24, previously `App.jsx`'s last live `supabase.from()` call site) — mirrors `ensureCohort.js`'s per-row completeness-check/repair pattern rather than a single "any row exists" flag, so a crash mid-seed self-repairs on the next mount instead of permanently under-seeding. No `UNIQUE` constraint exists on this table (unlike `cohorts`/`groups`/`time_blocks`) — two other files (`schema.sql`, `localDb.js`) have stale comments claiming otherwise, flagged as a follow-up cleanup, not yet fixed.
- **operations.host_seq** — new nullable column (schema migration v18, 2026-07-24). Persists the Host's canonical `seq` value on a Client's replicated copy of an op (`applyRemoteOp` in `syncClient.js`), fixing the `bulk_replace` cross-device seq bug — see Key Architectural Decisions.
- **schema_migrations** — `version, applied_at` — versioned migration guard table (currently **v55**). **v54 added `compound_cell_decisions`** (T118, see above); **v55 added `projection_failures`** (see above). **v48–v53 (lifecycle-IA / Roots-home / Special-Events / WS5 program, landed 2026-08-27→2026-08-30):** v48 added tile-world placement columns on `locations` (dormant spatial-layer seam, unrelated UI already removed at this point); v49 renamed `locations.tile_type` → `kind` and expanded its allowed-value set (same dormant seam); v50 introduced an indoor/outdoor `camp_maps` pair per camp plus a per-location map reference (also dormant — see the header note and Removed/Replaced); v51 added **`anchor_activities.kind`** (Fixed vs Recurring, table-recreate migration — see above); v52 added the **`open_reconciliation_decisions`** table (see above); v53 **dropped `template_overlays`** and `schedule_snapshots.overlays` (see above). Each v48–v51 table-recreate migration follows the same guarded, FK-pragma-restored, exception-safe (try/finally) shape; a rollback (`v4N_down.js`/`v5N_down.js`) exists through v53. **v39–v47 (unified schedule-overlay program):** v39 added `elective_set_activities.camper_headcount` (per-offering elective capacity); v40 added the **`events`** table + `template_slots.event_id`; v41 added the event internal-sub-schedule tables `event_time_blocks`/`event_groups`/`event_slots`; v42 added `anchor_activities.schedule_week_id` + `recurrence_level` (recurrence axis); v43 added six binding columns on `elective_sets` (`day_id`/`time_block_id`/`is_all_groups`/`group_ids`/`schedule_week_id`/`recurrence_level`); v44 added `activities.recurrence_truth_status`; v45 added `anchor_activities.location_id` + `events.location_id` (engine location-contention); v46 **DROPPED** the dead `day_override_templates`/`day_override_template_slots` pair (writer-less since T108); v47 added the host-local **`declined_two_row_splits`** table. Each v39–v45 ALTER appends its column LAST (column-order trap). Rollbacks `v3N_down.js`/`v4N_down.js` exist through v47 (`v46_down.js` deletes `schema_migrations WHERE version >= 46`, not `= 46`, so a stacked-migration rollback can't strand a higher version). v32 (M5) added `week_location_exclusions`; v33 (M6) added `camp_maps`; v34 (T40) added the `special_days`/`special_day_time_blocks`/`special_day_slots` family; v35 (T41) added `elective_sets`/`elective_set_activities` and the `template_slots.elective_set_id` column; v36 (T110) added `elective_sets.is_reusable`; v37 (T106) added `special_days.notes`; v38 (T108) added the `day_overrides` table + `schedule_snapshots.day_overrides_json`. Each of v36–v38 ALTER-adds its column LAST on a migrated db (the column-order trap noted in `schema.sql`). Rollbacks `v3N_down.js` exist for the recent ones. v19 added `audit_events`; v20 added device trust columns (`authorized_at`, `authorized_by_user_id`, `revoked_at`, `revoked_by_user_id`, `revocation_reason`, `device_secret_identifier`, `pairing_status`) to `devices`, the `host_signing_key` singleton table, and `camps.signing_public_key`. v26–v29 (reconciliation/onboarding program): v27 added **`schedule_weeks`** (`id, camp_id, name, sort_order, is_archived`) and `schedule_templates.week_id` (a template now belongs to a week, `UNIQUE(week_id, kind)` replacing the old per-camp uniqueness); v28 added **`week_activity_exclusions`** (`id, week_id, activity_id`, unique per week/activity) and **`week_group_exclusions`** (`id, week_id, group_id`, unique per week/group); v29 added **`operations.source`** (nullable `TEXT`) — per-field provenance (import vs. hand-edit) backing the S2a/S2b field-update-and-hand-edit-protection design. v30 (S1b) added the host-local **`source_aliases`** table (see above) + rollback `electron/db/rollback/v30_down.js`; v31 (B4) added the host-local **`import_evidence`** table (see above), also absent from every sync registry.
- **device_identity** — `id, created_at` — this device's own persistent identity, independent of camp/login state (exists even on a totally fresh install).
- **pending_writes** — durable backing store for a Client's offline write queue (`pending_id, client_write_id, entity, entity_id, field, value, parent_op_id, created_at`), so a queued write survives an app restart before it's confirmed applied.
- **locations** — `id, camp_id, name, capacity, notes, sort_order, map_geometry`, `UNIQUE(camp_id, name)`. Camp-scoped, ordinary op-log-synced entity (registered in `PROJECTIONS`, `campScopedEntities.js`, `full_sync`) — nothing host-local about it. See the Locations/Facility section below.
- **week_location_exclusions** — `id, week_id, location_id` — the third instance of the v28 week-exclusion pattern (`week_activity_exclusions`, `week_group_exclusions`, this one), added schema v32 (M5). Parent-keyed by `week_id`.
- **camp_maps** — `id, camp_id (UNIQUE), image_data, image_mime, image_width, image_height` — schema v33 (M6). ONE row per camp (`id = camp_id`, not a minted uuid), isolated from `camps` deliberately so an optional background-image blob never rides along with the near-every-screen `camps` read. `image_data` is always re-encoded JPEG, capped at ~1MB base64.
- **special_days** / **special_day_time_blocks** / **special_day_slots** — schema **v34** (T40 slice 1, ADR-level design `docs/work/specs/2026-08-20-special-days-data-shape-design.md`). A **one-day special-event schedule** (Maccabiah / color-war / trip / themed day) as its own entity family — ordinary op-log-synced, camp-scoped, NOT host-local. `special_days` (`id, camp_id, name, sort_order`, `UNIQUE(camp_id, name)`) is a standalone, un-dated, named single-day schedule; `special_day_time_blocks` (`id, special_day_id, name, sort_order, start_time, end_time`) is its OWN time grid (every special day owns its blocks); `special_day_slots` (`id, special_day_id, group_id, time_block_id, activity_id?, location_id?`) are the cells — columns are the camp's existing **groups** (reused, not throwaway teams), a cell holds an activity + optional location (no person/staff column — owner-scoped out). Children parent-scoped by `special_day_id`. `special_days` gained a **`notes`** column in schema **v37** (T106). Children parent-scoped by `special_day_id`; `deleteSpecialDay.js` cascades. **Author UI now shipped** (`SpecialDaysScreen.jsx`, T106/PR #132 — see Screens); ingest and Roots-Context wiring (T107) still deferred. The engine does NOT touch special days (authored, never generated).
- **elective_sets** / **elective_set_activities** + **template_slots.elective_set_id** — schema **v35** (T41 slice 1, design `docs/work/specs/2026-08-20-group-electives-design.md`). **Group-level elective scheduling** (no campers, no solver — human decides, app holds it). `elective_sets` (`id, camp_id, name, sort_order, is_reusable`, `UNIQUE(camp_id, name)`) is a named set of activity options ("Afternoon Chugim"); the **`is_reusable`** column (schema **v36**, T110) is the durability marker distinguishing a reusable set from a one-off (pre-v36 rows default reusable). `elective_set_activities` (`id, elective_set_id, activity_id`, parent-scoped join) its members. An elective is a **kind of cell content** — a nullable `elective_set_id` on the existing `template_slots` (mutually exclusive with `activity_id`) — inside the normal slot/route/sync machinery, NOT a parallel grid. The **engine SKIPS elective cells** (excluded from both head-selection and the span-tail collision check, mirroring anchors — `src/engine/buildSchedule.js` `electiveLookup`, threaded via `useGeneration.js`). `deleteElectiveSet.js` cascades. **Inline authoring now shipped** (T110/PR #132): a director creates/places an elective set directly from an empty schedule cell (point-of-intent), including the cross-device atomic-create invariant. Ingest wiring still deferred.
- **day_overrides** — schema **v38** (T108, ADR `docs/adr/2026-08-21-day-overrides-repoint-shape.md`). `id, camp_id, schedule_week_id, day_id, group_id, time_block_id, activity_id?, kind DEFAULT 'swap', note, created_at`, `UNIQUE(schedule_week_id, day_id, group_id, time_block_id)`. **REPLACES the old `day_override_templates` model** with a route-agnostic, **engine-blind, render-time diff**: an override row records "on this day, this group's cell at this block is swapped to a different activity (`kind='swap'`) or pulled empty (`kind='pull'`)", applied at render by `src/utils/applyDayOverrides.js` on top of whichever route's slots are showing — the base schedule and the engine never see it. Authored **in-grid** on ScheduleScreen via "override mode" (`useSlotMutations.js`; swap/pull/whole-day-pull), never destructive to the underlying slot. Upsert-by-coordinate (`src/utils/dayOverrideCoordinate.js`) reuses the existing row id so re-overriding a cell can't silently orphan/duplicate. Ordinary camp-scoped op-log-synced entity (registered in `PROJECTIONS`, the WS+IPC permission-matrix sweep maps, restore decisions, and undo FK edges). A span-tail cell refuses override (`isActivityTail`). Override undo/redo is a documented deferred gap (**T113**) — recovery today is whole-week snapshot restore. Orphaned-span-tail reconciliation is **T109**.
- **schedule_snapshots.day_overrides_json** — new nullable `TEXT` column (schema **v38**, T108). A snapshot now captures the whole week's `day_overrides` as JSON alongside `slots`/`overlays`, so restoring a snapshot restores its overrides too (`restoreSnapshotRows` 4th arg, `useSnapshots.restoreSnapshot`). Scoped JSON-text-blob exception, same rationale as `slots`/`overlays`.
- **events** / **event_time_blocks** / **event_groups** / **event_slots** — schema **v40/v41** (unified schedule-overlay, Events Slices 1–2). A first-class **event overlay** with its own internal sub-schedule. `events` (`id, camp_id, name, sort_order, notes, location_id`, `UNIQUE(camp_id, name)`) is a camp-scoped, op-log-synced entity placed on the campwide schedule as an opaque, span-mergeable cell via a nullable **`template_slots.event_id`** (mutually exclusive with `activity_id`/`elective_set_id`, mirroring the electives seam). The engine SKIPS event cells (override-and-replace). `event_time_blocks`/`event_groups`/`event_slots` are the event's OWN time bands, grid columns, and cells (parent-scoped by `event_id`, no `camp_id`) — a self-contained sub-schedule authored on the event drill-in (`EventScreen`). `events.location_id` (v45) feeds engine location-contention. `deleteEvent.js` cascades.
- **declined_two_row_splits** — schema **v47** (two-rows Slice 2a). `id, camp_id, activity_name_normalized, declined_at`, `UNIQUE(camp_id, activity_name_normalized)`. **Host-local, never synced** — deliberately absent from every sync registry, the same trust model as `source_aliases`/`import_evidence`/`host_signing_key`. Durable memory that the director declined a suggested two-rows split for a given activity name, so a re-import doesn't re-suggest it. Single writer `electron/ops/declinedSplits.js` (direct SQL, no `appendOp`; admin-gated at the IPC boundary; `recordDeclinedSplit`/`listDeclinedSplitNames` only — no delete/revoke path today).
- **compound_cell_decisions** — schema **v54** (T118, ADR `docs/adr/2026-09-03-compound-cell-interpretation.md`). `id, camp_id, pattern, interpretation ('as_written'|'wrapper'|'alternatives'), anchor_name?, wrapper_name?, confirmed_by?, confirmed_at`, `UNIQUE(camp_id, pattern)`. **Host-local, never synced** — same trust model as `source_aliases`/`import_evidence`/`declined_two_row_splits`. Durable memory of a director's confirmed interpretation of a compound schedule cell (e.g. "Lunch + Leave" = a wrapper around "Lunch"), so a later import at the same camp resolves the pattern silently instead of asking again. Single writer `electron/ops/confirmCompoundCellPattern.js` (host-only, admin-gated at the IPC boundary). Read at import time via `localClient` and threaded into the pure `extractEntities`/`fixedEvents`/`multiBlockCandidates` extraction functions as a plain lookup argument (no db access added to those functions themselves).
- **projection_failures** — schema **v55** (ADR `docs/adr/2026-09-04-projection-failure-detection-and-recovery.md`). `op_id (PRIMARY KEY, REFERENCES operations), entity, entity_id, field, error_message, failed_at, resolved_at?`. **Host-local, never synced** — deliberately absent from every sync registry (never added to `DOMAIN_SNAPSHOT_TABLES`, never written via `appendOp`), same trust category as `devices.last_synced_seq`. Records a projection failure on the remote-replication path: the op-log insert in `applyRemoteOp` (`electron/sync/syncClient.js`) succeeded but the corresponding `applyProjection`/`applyBulkReplaceProjection` call then threw, leaving that device's materialized table silently out of step with its own op-log. `op_id` is the primary key, so a repeated failure on the same op upserts in place rather than duplicating rows. Written and read via `electron/ops/projectionRepair.js`: `checkProjectionHealth(db)` lists unresolved failures (cheap, indexed on `idx_projection_failures_unresolved`); `repairProjectionForEntity(db, entity, entity_id)` replays that entity's ops in seq order to reconstruct its row, clearing the failure once it succeeds. Triggered automatically after LAN sync traffic quiets (a 500ms debounce on `op_applied` in `syncClient.js`, since the wire protocol has no explicit catch-up-batch-finished signal); otherwise reachable only via two MCP tools (`check_projection_health`, always available; `repair_projection_entity`, `--allow-write`-gated) — no renderer IPC, no director-facing UI in v1.
- **New columns on existing entities (unified schedule-overlay):** `activities.recurrence_truth_status` (v44, nullable `TEXT` — `'asserted'|'obligation'|'permission'|NULL`; NULL = not-yet-classified; written **create-only** by the ingest classifier, `src/ingest/truthStatus.js`, no engine/UI reader yet — data-shape-forward); `anchor_activities.schedule_week_id`/`recurrence_level`/`location_id` (v42/v45); `elective_sets` binding vector `day_id`/`time_block_id`/`is_all_groups`/`group_ids`/`schedule_week_id`/`recurrence_level` (v43) + `elective_set_activities.camper_headcount` (v39). All ordinary op-log-synced.
- **`anchor_activities.kind`** — schema **v51** (Fixed vs Recurring un-conflation, WS2, ADR `docs/adr/2026-08-28-fixed-vs-recurring-events.md`). `TEXT NOT NULL DEFAULT 'fixed' CHECK (kind IN ('fixed','recurring'))`, with a cross-column CHECK (`kind='fixed'` requires `is_all_groups=1 AND unit_id IS NULL AND group_ids` empty). SQLite can't attach a cross-column CHECK via `ALTER TABLE ADD COLUMN`, so the migration recreates the table (same shape as the v48–v50 `locations`/`camp_maps` rewrites). Backfill is classification-only, not a free default: an existing all-camp row backfills `'fixed'`, every group/division-scoped row backfills `'recurring'` — zero engine change (ADR §4). Powers the sidebar's two `AnchorsScreen` rows (`fixedevents`/`anchors`, see Screens).
- **open_reconciliation_decisions** — schema **v52** (ADR `docs/adr/2026-08-28-persisted-reconciliation-decisions.md`, PR #220). `id, camp_id, entity_type, cohort_id, entity_id, identity_key, kind ('confirm_value'|'confirm_change'|'resolve_conflict'), domain_key, child_key, entity_name, reason, import_run_id, created_at`. Written **only** from inside `commitPlan`'s commit transaction (`electron/ops/ingest.js`, host-only, admin-gated at the same import IPC boundary as the rest of that transaction) — an unresolved reconciliation decision from the last import now **persists** instead of vanishing once the import session ends. Read/dismissed via `electron/ops/openReconciliationDecisions.js` (host-only IPC). Feeds `RootsHomeScreen`'s "Needs your attention" list (`useOpenReconciliationDecisions`) — the mechanism that lets Roots surface stale/unconfirmed import decisions across sessions, not just during the import flow itself. Held conflicts are explicitly out of scope (they never reach the commit write).
- **day_override_templates / day_override_template_slots — REMOVED (schema v46).** This detached override-template pair (superseded by the render-time `day_overrides` diff at v38) never had a writer after T108 retired `DayOverridesScreen`; both tables were empty and are now dropped. They exist only in `v46_down.js` for rollback.
- **locations create path (T101 mitigation)** — not a new table, but note: deterministic location ids (`deriveLocationId`, T81/M4 ingest) now route through `resolveLocationCreateId`/`resolveLocationCandidateId` (`electron/ops/locationCreate.js` + `locationId.js`). On a rename-then-recollide (create "Pool" → rename → re-import "Pool") it mints a deterministically-disambiguated `${base}:${n}` distinct row instead of silently reusing/overwriting the renamed one. Cross-device convergent. Follow-ups: T103 (suffix namespace), T104 (shared free-suffix helper).

All tables above are local SQLite, read/written exclusively via `window.shoresh`/`localClient` IPC — no table is Supabase-backed anymore.

---

## Locations / Facility

Predates M1 in this document (this section did not exist before 2026-08-17) but has been live in the codebase since M4. A **location** is a physical place at camp (the Pool, the Gym, the Beit Midrash) with a `capacity` — how many groups can use it at once. Optional: a camp that never assigns activities to locations has zero rows and nothing about the app requires them.

- **Screen**: `locations` in the `SCREENS` map, `src/screens/LocationsScreen.jsx` — an inline-add-table setup screen (PR #246), matching the pattern used across the other setup screens: an inline blank-row add (name, capacity, kind) as the last table row, plus Excel import (with a downloadable template) for bringing in many rooms at once — replacing the screen's older bulk "+ Add N rooms" seeder and card-form add. Columns are Name / Groups at once (capacity) / Kind / Notes, with notes edited in-row after creation. Also carries per-week availability (the toggle column, M5). The optional camp-map background image and pin-placement UI were **removed with the spatial layer (PR #201)**; the screen no longer renders a map.
- **Engine capacity source**: the schedule engine (`src/engine/buildSchedule.js`) reads `locations.capacity` as the authoritative contention limit for any activity assigned to that location — replacing the earlier free-text `activities.location` string with a real, capacity-bearing entity. The order-dependent capacity bug that motivated the `locations` entity (two activities silently allowed into the same room because location was just a label) is closed by this.
- **Per-week availability (M5)**: `week_location_exclusions` (`week_id, location_id`) marks a location closed for a specific week — "the lake is closed weeks 1 and 2" — mirroring the existing `week_activity_exclusions`/`week_group_exclusions` pattern. Honored on both schedule-building routes (manual and generated) plus at anchor placement, matching how activity/group week exclusions are already honored (see `docs/adr/2026-08-17-...` history and the manual-route week-exclusions work).
- **Camp map + Day Map — REMOVED (PR #201, commit `335e311`).** The entire spatial layer built in PRs #182/#183/#196/#199/#200 — the optional camp-map background (`camp_maps`), in-place pin placement, the Locations Map tab + drag-to-place Canvas, the Tile-World simulation, the read-only **Day Map** view (`schedule:map`, `DayMapScreen`), and `src/data/deriveOccupancy.js` — was deleted; the owner judged it "didn't fit our infrastructure." The schema columns (`camp_maps`, `locations.map_geometry`/`map_id`/`kind`/`grid_x`/`grid_y`) are **kept dormant as a rebuild seam** but nothing reads them for rendering. A future schedule-driven map (on a real aerial with top-down pixel assets) is a separate Phaser-based initiative, not part of this app's current surface. The engine never read `map_geometry` and still doesn't (`mapOptionalInvariant.test.js`).
- **Ingestion integration**: `locations` is a full `INGESTIBLE_ENTITIES` member (`src/ingest/extractEntities.js`) and an `ALWAYS_SCANNED_ENTITIES` member (`src/ingest/existingSnapshot.js`) — the one exception to Replace mode's "six schedule-content entities skip recognition" rule (M4 §D2): locations are durable and never torn down by Replace, so they are always scanned live regardless of mode. An imported activity's free-text location column resolves through `resolveOrCreateLocationId` (`src/ingest/fieldUpdate.js`) — matches an existing location by name or proposes creating one, never silently drops the column. `buildBlastRadiusIndex` (`src/ingest/blastRadius.js`) counts `activities.location → locations` as one of its two concretely-wired FK-shaped references, so a location referenced by many activities carries real weight in the reconciliation screen's readiness-demand ordering.
- **`ReconciliationScreen` now the reconciliation UI**: `src/screens/ReconciliationScreen.jsx` (R2'b, one-screen reconciliation rebuild) replaces the `import` screen's post-preview reconciliation half — `ReconciliationQueue.jsx`, `ReconciliationSummary.jsx`, `ReconciliationLedger.jsx`, and the held-conflict takeover modal are deleted; a Facility domain filter chip surfaces `locations` decisions honestly (count `(0)`, no warning tint) even for a plain spreadsheet with no location columns at all. See `docs/adr/2026-08-17-onescreen-reconciliation-projection.md` and `docs/work/specs/2026-08-17-reconciliation-onescreen-design.md`.

---

## Project Lifecycle

Managed by `electron/db/projectManager.js`. A "project" is a named SQLite file on disk.

**IPC surface** (exposed via `electron/preload.js`, handled in `electron/main.js`):

| IPC channel | Description |
|---|---|
| `shoresh:create-project` | Create a new SQLite db file at a chosen path, run schema migrations, bootstrap a camp |
| `shoresh:open-project` | Open an existing `.shoresh` db file |
| `shoresh:backup-project` | Write a timestamped backup copy of the current db |
| `shoresh:restore-project` | Replace the active db with a backup (pre-restore backup is automatically taken first) |
| `shoresh:export-project` | Export a copy to a user-chosen location |
| `shoresh:list-recent-projects` | Return the MRU list of recently opened project paths |
| `shoresh:open-recent-project` | Open a project from the recent list |
| `shoresh:get-current-project` | Return metadata (path, name) for the currently open project |

**Backup rotation**: maximum 10 backups kept per project; oldest are pruned automatically. Pre-migration and pre-restore backups are taken automatically before any destructive operation.

---

## Test Coverage

- **Vitest unit tests**: **317 test files, 4,641 tests passing (2 skipped)**, a clean full-suite run confirmed 2026-09-04 — auth, ops, projections, sync handlers, IPC handlers, schedule engine, ingest, plus the events/electives/recurrence/location-contention/projection-repair/inline-add-setup-screen surfaces. Run with `npm run test`.
- **Integration test harness**: `test/integration/` — **27 separate-process scenarios** driven by `node test/integration/run.js`. Each scenario spawns real Node child processes (not Electron, but using the same `electron/` modules) to verify cross-process behavior that single-process Vitest cannot distinguish from correct behavior. Scenarios cover: bootstrap, offline restart, idempotency, conflict detection, device revocation, seq catch-up, pairing reconnect, field-merge ordering, lock expiry, snapshot restore, schema migration, host crash mid-sync, corrupt payload rejection, clock skew, role-change enforcement, second-device domain sync, restore queue, retiring orphan slots, deleting a used record, prior-year ingest, location merge, camp-map sync, and full-sync manifest coverage for week-location-exclusions / special-days / electives.

---

## Edge Functions / API Routes

No Supabase Edge Functions or HTTP API routes — the local-first architecture has no server. All device-to-device communication is the custom WebSocket protocol in `electron/sync/syncServer.js` / `syncClient.js` (message types include `authenticate`, `login`, `acquire_lock`, `submit_op`, plus server→client `login_ok`/`login_failed`, `op_applied`, `op_conflict`, `full_sync`).

**Machine-facing surface (not an HTTP API).** A headless **MCP server** (`scripts/mcp/server.js` + `tools.js`, launch `npm run mcp -- --db <path>`, stdio, read-only by default with an `--allow-write` gate; ADR `docs/adr/2026-08-21-mcp-ingestion-server.md`, plan `docs/work/plans/2026-09-01-machine-access.md`) exposes domain verbs over a camp's SQLite file with no Electron/IPC/auth: `ingest_preview`, `ingest_commit`, `list_entities`, `setup_summary`, `schedule_state` (**the validate verb** — re-runs the pure engine over stored placement, returning the same findings/conflicts the Schedule screen shows; note the engine's `conflicts` array is always `[]` today — cross-cohort shared-resource contention is not yet computed, so an empty `conflicts` result means "not checked," not "checked and clear"; see `src/engine/buildSchedule.js:23`), `export_schedule` (the stable **`format_version: 1`** JSON schedule export), and two support/debugging tools added 2026-09-04 for the projection-failures ledger (see Database Tables): `check_projection_health` (read-only, always available — lists this device's unresolved projection failures) and `repair_projection_entity` (`--allow-write`-gated — re-derives one entity's row from its full op-log history). Neither of the latter two is director-facing. The same portable JSON export is available in the GUI (`src/utils/exportScheduleJson.js`'s `buildScheduleExport`, shared with the Excel export via `src/utils/scheduleCells.js` so the two can't drift; "Export data (JSON)" on the Schedule toolbar). The custom WS sync protocol is device-to-device plumbing, deliberately **not** exposed as a public API.

---

## Key Architectural Decisions

- **Ed25519 Host-only token minting**: camp tokens (used for network authentication) are signed with an Ed25519 private key that never leaves the Host. The public key (`camps.signing_public_key`, hex DER/SPKI) is distributed to Clients via full-sync so they can verify tokens but never mint them. Client offline sessions use per-device HMAC-SHA256 local tokens keyed to a `device_secret_identifier` minted by the Host at pairing. This replaced the original shared HMAC signing secret (`camps.signing_secret`) that was distributed to all devices — that earlier design meant any compromised device could forge tokens accepted by all others. See Auth section above for full details.
- **Raw PIN sent over LAN for remote login**: a fresh Client verifies its PIN against the Host by sending it in plaintext over the WebSocket (`login` message), rather than a hash — necessary because the Host must run its own `scryptSync` check. Accepted under the same trusted-LAN threat model; flagged for revisit if camps ever share network with untrusted devices.
- **Unified login path**: both local (offline fallback) and remote (online, incl. first-ever login) logins go through one `attemptLogin(db, ...)` function, so lockout/verification logic can't drift between the two call sites.
- **Op-log + last-write-wins with explicit conflict table**: all mutations are appended as `operations` rows (entity/field-level), synced and replayed across devices; genuine conflicting writes are recorded in `conflicts` (not silently dropped) and must be explicitly resolved, with resolution ops linked via `parent_op_id`.
- **Single-camp-per-db assumption**: every `camps` lookup in the codebase does `SELECT ... FROM camps LIMIT 1` rather than filtering by an active-camp id — a device's SQLite file only ever holds one camp's data. This is a real constraint, not an oversight — changing it (e.g. to support multiple camps per device) would require auditing every one of these call sites.
- **Centralized authorization layer** (Phase 2, 2026-07-24/25, 4 tasks, ADR at `docs/adr/2026-07-24-centralized-authorization-layer.md`): replaced four scattered inline `role !== 'admin'` checks (and a completely unchecked WS layer) with one central `authorize()` primitive re-deriving role from the DB on every call, a named permission matrix, and shared action-derivation logic reused identically by the IPC and WS transports. See Role-Based Behavior above for the full shape. Explicitly out of scope for this phase (deferred): device pairing/revocation, token expiration/lifetime, any change to the shared-camp-signing-secret token-minting model, raw PIN transmission changes, full audit-event-stream infrastructure (a single `console.warn` on a denied `authorize()` call is the only logging added).
- **`bulk_replace` op primitive**: a `{entity, scope_id, rows}`-shaped op that deletes all current rows in scope and inserts a new set, atomically, recorded in the op-log like any other op. Built in Sub-plan A Task 3 (`appendBulkReplaceOp`/`applyBulkReplaceProjection`/`validateBulkReplaceRows` in `electron/ops/operations.js`), extended with a renderer-facing IPC channel and a second (`template_overlays`) registry entry in Sub-plan E Task 3. **`template_overlays` was retired at schema v53** (ADR `docs/adr/2026-08-30-retire-overlay-stamp-subsystem.md`), so `BULK_REPLACE_ENTITIES` (`electron/ops/operations.js`) now registers only `template_slots`. Used by `ScheduleScreen.jsx` for schedule regeneration and snapshot restore — the only consumer. As of Phase 2, admin-only via `<entity>.bulk_replace` on both the IPC and WS submission paths.
  - **FIXED 2026-07-24** (previously CRITICAL, found by Sub-plan E Task 5's end-to-end verification, `electron/sync/scheduleE2E.sync.test.js`, originally left as an honest `it.fails` regression test — now un-skipped and genuinely green): `applyRemoteOp` in `electron/sync/syncClient.js` inserted a received op without persisting its Host-assigned `seq` (`electron/db/schema.sql`: `seq INTEGER PRIMARY KEY AUTOINCREMENT`) — each device's local `operations.seq` was that device's own independent counter, not the Host's canonical one. `latestScopeOpSeq`/`detectBulkReplaceConflict` (`electron/ops/operations.js`) compared a client's locally-numbered `based_on_seq` directly against the Host's own seq counter as if they were the same numbering space, so the SECOND `bulk_replace` to the same scope from the same client (build a schedule, then click Regenerate) spuriously conflicted in essentially all real usage. **Fix** (full Architect + Maker + review cycle, ADR at `docs/adr/2026-07-24-bulk-replace-seq-fix.md`): added a nullable `operations.host_seq` column (schema v18); `applyRemoteOp` now persists the Host's real `op.seq` into it on insert; `latestScopeOpSeq` compares `MAX(COALESCE(host_seq, seq))` instead of raw `seq`, so Host-authored rows (host_seq always NULL, own seq already canonical) and Client-replicated rows (host_seq populated) share one numbering space. `isValidRemoteOp` now requires `op.seq` be a non-negative integer (fail-closed). Disclosed, accepted residual limitation: operations rows from before this migration have `host_seq = NULL`; if Client-authored, scope-seq comparison falls back to that row's own (possibly wrong) local seq until superseded — at most one spurious conflict per scope post-upgrade, then self-healing (mathematically bounded, verified by Red Hat review), no backfill migration needed.

---

- **Host-local aliases, never synced** (S1b, schema v30, ADR `docs/adr/2026-08-09-s1b-host-local-aliases.md`): confirmed source→entity aliases live in a host-local `source_aliases` table that is deliberately absent from every replication registry — the same trust model as `host_signing_key`. Chosen over a synced/replicated alias design (two earlier ADRs, now superseded) because it dissolves an entire class of cross-device divergence/replay complexity: there is exactly one writer (`confirmAlias.js`, host-only + admin-gated), so alias identity can't be forged over the wire or diverge between devices. Trade-off accepted: aliases are per-host, not shared to Clients — fine because import is already host-only.
- **Re-import provenance protection (Policy A), one shared channel** (S2a/S2b + 2026-08-09 ingest fixes): a director's hand-edit is stamped `operations.source = 'human'`; an import writes `'import'`. On re-import, a field whose latest op is `'human'` is not silently overwritten — it holds a stale conflict for review (`isProtected = !!latestOp && latestOp.source !== 'import'`). Review-time hand-edits (a unit assignment/clear, a group field, an activity rule) reach this via ONE generic side-channel: the renderer sends `humanEditedFields: { <entity>: { <name>: [fields] } }` through the host-trusted `shoresh:ingest-commit` IPC path only; `buildPlan` attaches it per-item as `_humanFields`; `commitCreate`/`commitUpdate` stamp those fields `'human'`. This channel is unreachable from the WS `submit_op` path, so it adds no forgery surface. All three provenance gaps (units/tiers, groups, activity rules) are closed by this one mechanism.
- **Fixed events route to the schedule, not the activity catalog** (2026-08-09, ADR `docs/adr/2026-08-09-ingest-fixed-event-routing-and-reviewable-units.md`): a name pinned across the grid (Lunch, Mifkad, Sign-In) was being written BOTH as a catalog `activities` row and as `anchor_activities`. It now defaults OUT of the catalog. Critically, this is a **visible, reversible review decision**, not a silent engine drop — `inferFixedEvents` returns `dualUseNames` only as a SEED for the import review's tick-defaults (pin-only rows default unticked with a note + one-click override; genuinely dual-use names default kept, flagged). `buildPlan`/`commitPlan` are unchanged and golden-ops stay byte-identical. This deliberately avoids letting the fixed-event detector's intentional over-inclusion silently delete a legitimate catalog activity.

---

## Home Screen Layout

Not role-differentiated at the top level, and **stage-aware** (ADR `docs/adr/2026-08-28-stage-aware-nav-landing.md` Decision 1): the landing screen is **`seed`** (`src/screens/SeedScreen.jsx`) for a brand-new camp with no setup data, and **`roots`** (`src/screens/RootsHomeScreen.jsx`) for every other camp, regardless of `admin`/`staff` role (`App.jsx`'s `AppShell`, `useState(() => campIsEmpty ? 'seed' : 'roots')`). `RootsHomeScreen` **replaced** the earlier `ReconciliationScreen`-in-`mode="inspect"` roots screen (ADR `docs/adr/2026-08-28-roots-home-is-a-distinct-screen.md`, WS4) — it carries no census/diff vocabulary and no readiness-verdict banner (the six-state verdict from the retired `ReadinessHub`/old Roots banner is deliberately not carried forward; Schedule is presented as a plain door). No per-role home screen variants found. The setup entity screen itself is `camp` (`src/screens/CampScreen.jsx`), reached from the sidebar's gear admin menu (not from RootMap nodes any more — RootsHomeScreen carries no RootMap), not the landing screen. `navSections.js` no longer lists a `readiness` sidebar item; `roots` is the fixed top nav row, and `TopBar` shows a "← Roots" return affordance on setup screens (Shell threads `onNavigate`).

**Read-only invariant (Roots):** `RootsHomeScreen` is structurally read-only — it reads live structure counts and persisted open reconciliation decisions, with no write path reachable from the screen. **Note:** the earlier "post-import grace-window undo" affordance (a banner shown on Roots after a finished import, letting a director undo it within a time window) is **no longer wired up** — `App.jsx` no longer carries a `justImported` outcome, and `src/components/reconciliation/postImportBanner.jsx` (the banner component) and `src/hooks/useGraceWindowUndo.js` are not imported anywhere outside their own test files. Grace-window undo, if still needed, would need to be re-homed inside the import flow itself (`ReconciliationScreen`, which still carries the underlying `ingestUndo`/`invertibleOps` plumbing) or rebuilt for the new Roots screen — flagged here as a verified regression/gap, not confirmed intentional.

---

## What's changed since 2026-07-26

**Projection-failure detection and recovery (schema v55, 2026-09-04).** ADR `docs/adr/2026-09-04-projection-failure-detection-and-recovery.md`. Closes a gap where a device's local tables could silently and permanently diverge from its own op-log: if a replicated op's insert into `operations` succeeded but the follow-on projection into a live table then threw (`applyRemoteOp` in `electron/sync/syncClient.js`), the failure was only logged to the console, with no record and no recovery path. A new host-local, never-synced `projection_failures` table now records each such failure; `electron/ops/projectionRepair.js` adds a read-side check (`checkProjectionHealth`) and an entity-scoped repair (`repairProjectionForEntity`, replays that entity's ops in seq order to rebuild its row). Repair runs automatically on both Host and Client after a short debounce once sync traffic quiets, and is otherwise reachable only through two new MCP support-surface tools (`check_projection_health`, `repair_projection_entity` — the latter `--allow-write`-gated); there is no renderer IPC and no director-facing UI for this in v1. See Database Tables and Edge Functions/API Routes above.

**Ingest asks the director to interpret compound schedule cells (T118, schema v54, 2026-09-03).** ADR `docs/adr/2026-09-03-compound-cell-interpretation.md`. Pressure-testing ingestion against real camp files surfaced a class of cell — `Lunch + Leave`, `Change/Snack`, `Climbing/Sports` — that the importer previously read wrong with HIGH (silent) confidence, since the confidence model conflated "seen reliably" with "understood correctly": one real file produced a fabricated "min 6 / max 8 times per week" rule for a cell that was never a real activity, cascading into 2,233 spurious UNDERSERVED findings. Four slices shipped: (1) a new pure classifier (`src/ingest/compoundCellPatterns.js`) flags a candidate compound pattern using a partner-diversity test (a word pairing with ≥2 distinct partners is a wrapper/rotation candidate; a word that only ever pairs with the same partner, e.g. "Arts & Crafts", is one fixed name — no card generated) plus a standalone-anchor test to guess which side is the real activity; (2) a new host-local, never-synced `compound_cell_decisions` table (schema v54) persists a confirmed interpretation per camp, sibling to `source_aliases`/`declined_two_row_splits`; (3) the camp's confirmed decisions are read at parse time and threaded into the pure `extractEntities`/`fixedEvents`/`multiBlockCandidates` functions as a plain lookup, exactly parallel to the existing `canonicalMap` pattern — no db access added to those functions; (4) a new "Cells We Weren't Sure About" section on `ImportScreen.jsx` (same visual family as "Longer Blocks", nothing pre-selected) asks the director once per distinct pattern with four choices — one thing as written / a wrapper around [anchor] / co-equal alternatives / not sure yet — and a resolved "wrapper" verdict keeps the wrapper string out of the activity catalog and fabricates no rule for it. Never auto-splits or auto-merges a cell; a genuinely ambiguous wrapper-direction guess is withheld rather than guessed (follow-up fix after the initial slice). Out of scope for v1: the separate PDF-multi-line-merge parsing gap, and PDF import itself (not supported today).

**Imported-schedule-as-a-version + typo-dedup sweep completed (T117, 2026-09-02).** No schema change (still v53).
- **Import materializes a saved version** (ADR `docs/adr/2026-09-02-imported-schedule-materializes-as-a-version.md`, T117 slices 1–2). `src/ingest/capturePlacements.js` (slice 1) walks the parsed import grid and returns the actual (group, day, block) → activity/anchor cells it sees; `electron/ops/resolveImportedPlacements.js` + `electron/ops/materializeImportedVersion.js` (slice 2) turn those into ONE `schedule_snapshots` row — named "Imported schedule", on the manual route's `schedule_templates` — written host-side immediately after `commitIngest` returns (not inside its transaction, so `commitIngest`'s six-entity create whitelist stays untouched). The imported version never auto-fills the working grid; a director pulls it up from the Schedule screen's version history like any hand-saved snapshot ("grid = workbench, versions = woodpile"). A placement whose name can't be resolved against the just-committed catalog is counted and reported on `ImportScreen.jsx`'s post-commit banner, not silently dropped. Re-importing (Add or Replace) always adds another version rather than replacing the last one. Three v1 limitations are recorded in the ADR as accepted, not fixed: anchor-first name resolution on a dual-use name, an anchor's day/group scope isn't verified against the cell it lands in, and a narrow first-import concurrency window when the manual template row must be minted.
- **Restore-time reference guard** (same window, Red Hat-flagged HIGH). `useSnapshots.js`'s `restoreSnapshot` now filters out any saved slot whose group/day/time-block/activity/anchor id no longer exists in the live catalog before writing it back to the grid, reporting a skipped-cell count ("N cell(s)...were skipped") to the director — closing a gap where a Replace re-import (which mints new catalog ids) could make restoring an older version silently write a broken/blank grid. Applies to every saved version, imported or hand-made.
- **Typo-insensitive duplicate detection, sweep completed.** The shared whitespace/case-insensitive name-matching key is now applied at every remaining create-vs-match gate in the app — the schedule grid's inline cell editor (`CellInlineEditor`), the Activities screen (add/copy/quick-add/bulk-paste), Electives (`addSet`), and Special Events (`createEvent`/`createDay`) — so a typo like extra spacing or different case never mints a duplicate entity where an existing one already matches. Distinct word-forms (e.g. "Swim Return" vs. "Swim Returning") still stay distinct.

**Lifecycle-IA nav redesign + Roots home as a distinct screen + Special Events unification + schedule-screen (WS5) rebuild (schema v48–v53, PRs #209–#242, 2026-08-27 → 2026-08-30).** Five arcs, following the north-star roadmap `docs/work/specs/2026-08-28-lifecycle-ia-program.md` ("Seed → Germination → Sprouts → Roots → Plants"):
- **Stage-aware navigation + landing** (WS1, ADR `docs/adr/2026-08-28-stage-aware-nav-landing.md`, PRs #211/#212). The sidebar's old two-section "Camp Set Up"/"Schedule" model is replaced by a fixed, chevron-less **Roots** row plus three collapsible stages — **Germination** / **Sprouts** / **Plants** (`navSections.js`'s `NAV_SECTIONS`); Plants is **pinned**, never collapsible, so the active-route highlight stays visible. Camp/Re-import/Conflicts/Trash moved into a gear-button admin menu; Devices ("LAN & Devices") stays a separate admin-only entry. A brand-new camp with no setup data lands on a new `SeedScreen` (`seed` key) instead of Roots.
- **Fixed vs Recurring events un-conflation** (WS2, ADR `docs/adr/2026-08-28-fixed-vs-recurring-events.md`, PRs #210/#213, schema v51). `anchor_activities` gained a `kind` CHECK column (`'fixed'|'recurring'`); the sidebar now has two rows — "Fixed Events" and "Recurring Events" — both routing to the same `AnchorsScreen`, filtered by a fixed `kind` prop, not two screens.
- **Persisted reconciliation decisions → Roots attention list** (ADR `docs/adr/2026-08-28-persisted-reconciliation-decisions.md`, PR #220, schema v52). A new host-scoped `open_reconciliation_decisions` table durably records an unresolved import decision past the end of the import session, so it can resurface as a cross-session "needs a look" item rather than vanishing.
- **Roots home is a distinct screen** (WS4, ADR `docs/adr/2026-08-28-roots-home-is-a-distinct-screen.md`, PRs #214/#215/#217/#218/#219/#220). New `RootsHomeScreen.jsx` **replaces** the `roots` route (previously `ReconciliationScreen` in a fixed `mode="inspect"` prop) — no census/diff vocabulary, no RootMap/RootMapPanel/verdict banner. It shows a live-structure-counts bento plus a "Needs your attention" list unioning the new persisted decisions with live structure checks. `ReconciliationScreen`'s census/RootMap surface is now scoped to the import-a-file flow only ("census stays import-only"). Side effect (verified, not confirmed intentional): the old post-import grace-window-undo banner is no longer wired into any live screen — see Home Screen Layout and Removed/Replaced.
- **Special Events unification** (owner-approved 2026-08-29, ADR `docs/adr/2026-08-29-unify-special-events-screen.md`, PRs #235/#242). The separate `EventScreen`/`SpecialDaysScreen` Sprouts authoring rows merge into one `SpecialEventsScreen.jsx` (`specialevents`) — a single type-tagged table of `special_days` + `events`, capped by an **inline-add** row (Wave C1, PR #242) rather than a modal. The Plants-side build surfaces (`SpecialSchedulesScreen`) are unchanged.
- **Schedule screens rebuilt** (WS5, PRs #221–#229/#236–#238). Pinned Plants sidebar rows (#221); a slimmed toolbar (#222); the activity palette reshaped into a filterable **Ledger** (#223); **Daily View restored as the default** with cell merging (#224, was Group View); double-click-to-edit a grid cell via `CellInlineEditor` instead of a separate modal (#225); an overflow popup replaced by a quiet visible cluster (#229); stat badges hide when their target is unconfigured (#228). The dead `template_overlays`/`schedule_snapshots.overlays` pair was dropped at **schema v53** (ADR `docs/adr/2026-08-30-retire-overlay-stamp-subsystem.md`) — no live writer had existed since T108.
- Also in this window: several whole-app "design coherence" passes (PRs #230–#234/#238) consolidated the colored-pill chip shape and caution/error banner styling into shared style tokens and added shared `InlineAddRow`/`ScheduleDoor` primitives; a `graphify` codebase knowledge graph was wired into the repo (PRs #226/#227, tooling only).

See the header note, Navigation Model, Screens, Database Tables, Components, and Home Screen Layout sections for the current shape of all of this.

**Spatial layer removed + Roots hub redesign (PRs #201/#204/#205/#206, 2026-08-27).** (1) The entire "run the day on the map" spatial layer — Day Map, Locations Map tab, drag-to-place Canvas, Tile-World sim, `deriveOccupancy.js` — was **deleted** (#201, commit `335e311`); `LocationsScreen` is list-only and the v50 map columns are kept dormant as a rebuild seam. (2) The Roots census/domain grid became a responsive **Bento** (#204) and was then **demoted to the Understood tile's drill-down** when the **census tiles became the primary interface** (#206, ADR `2026-08-27-roots-hub-tiles-are-interface.md`); the banner lost its dead "Facility map" button. (3) The manual **Sort Order** input was retired in favor of a derived order (#205). See the header note, Screens (`schedule:map` retired), Locations/Facility, and Removed/Replaced.

**Unified schedule-overlay program + Roots restructure + Day Map (schema v39–v47, merged through PR #183, 2026-08-22 → 2026-08-25).** *(The Day Map arc below was subsequently removed in PR #201 — see the entry above.)* Three arcs:
- **Unified schedule-overlay model** (ADRs `2026-08-23-unified-schedule-overlay-model.md`, `2026-08-23-activity-recurrence-tiers-ingestion.md`, `2026-08-23-override-family-model.md`). The five "special things on the schedule" are one family on a **contend-and-coexist vs override-and-replace** seam. Shipped in sliced PRs: **Events overlay entity** with an internal sub-schedule (#149/#150, v40/v41); **Event/Electives schedule import** via a shared grid parser — "surface globally, build locally" (#152/#154); **Fixed→Recurring Events** rename + daily/weekly recurrence axis on anchors (#157/#158, v42); **elective binding vectors + capacity** (#146/#159, v39/v43); **truth-status ontology** `activities.recurrence_truth_status`, classifier writes create-only (#160–#163, v44); **engine location-contention** — anchors/events/electives register their location in the capacity map so a booked room reads full and the engine routes around it, with a `LocationPicker` on both screens (#164–#166, v45); **two-rows split suggestion** with host-local decline-memory (#172–#177, v47); **merged-cell ingest** reading multi-block spans from XLSX `!merges` (#179/#180). The dead `day_override_templates` pair was dropped (#167, v46). The review loop repeatedly caught silent bugs green unit tests missed (double-classification, span overflow, a 14-duplicate aggregation surfaced only by running detectors against real camp files) — real-data validation against `~/Desktop/camp schedules/` is now a standing check.
- **Roots dashboard restructure (PR #181).** The census **Context domain was removed** (5 domains → 4: Structure / Scheduling / Time / Facility); Events / Special Days / Electives fold into the **Scheduling** box via the normal `DOMAIN_OF`/`CHILD_OF`/census machinery (net −160 LOC). `CENSUS_ENTITIES` gained `events`/`special_days`/`elective_sets`; nodes route to their entity setup screens (optional, not required).
- **Locations "run the day on the map" (PRs #182/#183, ADR `2026-08-24-run-the-day-on-the-map.md`).** **Create-in-place** — click an empty spot on the camp map, name it, and the location is created there with `map_geometry` (#182). **Day Map** (`schedule:map`, `DayMapScreen`) — a read-only spatial view where each group sits at its activity's room per block and over-capacity rooms light up as traffic jams, derived read-only via `deriveOccupancy.js` with no engine call; a located-but-unpositioned room's groups (and any hidden jam) surface in a "Not on the map" panel (#183). Follow-ons B2 (animate the day) and B3 (drag-to-reschedule) are not yet built.

**Electives / special-days / day-overrides audit program (PR #132, merged 2026-08-21).** An architecture/product audit (`docs/work/specs/2026-08-20-electives-specialdays-facility-audit.md`) of three "does the director work at the point of intent?" capabilities produced **four ratified ADRs** — `2026-08-20-in-context-knowledge-and-durability-tiers.md` (the one-off / this-summer / durable spectrum, tier-(c) routing to census vs Roots Context), `2026-08-20-electives-authoring.md`, `2026-08-20-special-days-authoring-and-day-override-repoint.md`, `2026-08-21-day-overrides-repoint-shape.md`, and `2026-08-20-facility-topology-foundation.md` (ratified, build deferred) — and **three implemented areas**: (1) **electives authoring** (schema v36 `is_reusable` durability marker + inline create-from-empty-cell, cross-device atomic-create invariant — T110); (2) **special-days author UI** (schema v37 `notes` + standalone `SpecialDaysScreen` — T106); (3) **day-overrides re-point** (schema v38: retired the old `day_override_templates` editor for a route-agnostic, engine-blind, render-time `day_overrides` diff authored in-grid, + snapshot participation via `day_overrides_json` — T108). The review loop caught several silent bugs invisible to green unit tests (override-overwrite data-loss surfaced only on real SQLite; missing permission-matrix/restore/undo registration; a WS-sweep hang from an unmapped writable field). Deferred follow-ups tracked on `main`: **T107** (special-days → Roots Context), **T109** (orphaned span-tail guard), **T113** (override undo/redo), and the **facility-topology** build.

**Roots-as-dashboard/spine (PR #113/#114, merged 2026-08-20).** Roots became the app HOME. Five TDD tasks: T1 promoted RootMap nodes to a primary "Manage {Area} →" navigation (`rootMapNav.js`, dangling-target-guarded); T2 added the `RootsBanner` — a single-source readiness verdict (`getReadiness`/`describeReadiness`) plus Import/Worksheet/Facility entry points; T3 made `roots` the landing screen, redirected the `readiness` key to it, and **retired `ReadinessHub`** (deleting `ReadinessHub.jsx` + its test + `readinessHubModel.js`, and the orphaned `importOutcomeModel.js`); T4 routed a finished import back to Roots with a `postImportBanner` + a surviving grace-window undo (App-level `justImported` carrier, cleared at the navigation edge); T5 added the "← Roots" return affordance to setup screens. No schema change. Design `docs/work/specs/2026-08-19-roots-dashboard-spine-design.md`, plan `docs/work/plans/2026-08-19-roots-dashboard-spine.md`. Header/heading copy is deliberately plain (parked for a later language pass). The north star (absorbing Camp Setup editing into the node panels themselves) is explicitly future work — see the design doc's "Architecture toward the north star". The M2–M5 reconciliation-depth items were filed as tickets T95–T98 (PR #115).

**Schedule canvas rebuild (T50–T60, merged 2026-08-01).** `ScheduleScreen.jsx` migrated from a hand-rolled CSS table to a true CSS Grid canvas (`role="grid"` frame, `grid-row: span N` for merged cells). The grid is the source of truth for layout; `scheduleGrid.css` covers cell pseudo-states and data-attribute states (one scoped stylesheet exception, boundary is `src/components/schedule/`). Three post-canvas bugs fixed: anchor double-scheduling (T62 — engine now excludes anchor-referenced activity_ids from the placement pass), JSON-string group_ids parsed at the IPC boundary not inside the engine (T63, T69 — engine contract is array-only, enforced by a DEV-only `assertIdListShape` assertion, T71), and stats bar accuracy (T65 — `'unavailable'` slots excluded from Placed denominator, dead engine `stats` object deleted).

**Atomic replace-ingest (T61, 2026-08-07).** `commitIngest` runs entirely inside one `db.transaction()`. Teardown order is normative (8 steps, spec at `docs/work/specs/S-replace-ingest-atomic-transaction.md`), `PRAGMA foreign_key_check` runs after deletes, rollback is proven by test. Host-only guard on both Add and Replace modes. Activity `min_per_week` floored to 1 for eligible activities on import. Replace confirmation names every category of director-authored content destroyed: schedule slots, Fixed Events (T68 — with live count, recoverable from Trash), saved versions (irreversible, visually distinguished in `--danger`), Day Override templates. Warning block is a rendered array, not pairwise `marginTop` conditionals.

**`appendOp` performance (T66, 2026-08-08).** Four `db.prepare()` calls per op (~1.3ms each, ~15ms/row) now prepared once per `db` handle via `WeakMap<db, Map<sql, Statement>>` in `electron/ops/stmtCache.js`. A 2400-op Replace ingest dropped from ~37s to non-blocking. Op-log semantics unchanged.

**Design polish pass (design/polish-20260807, merged 2026-08-07).** Schedule screen conformance to `DESIGN_STANDARD.md §5/§8`: global motion tokens (`--motion-quick`, `--motion-base`, `--motion-slow`, `--ease-out`, `--ease-in-out`) in `src/index.css`, `useEnterTransition` hook, error banner entry motion, activity palette token reconciliation, and cross-screen polish (Waves 0–4).

**Dev environment (T64, 2026-08-07).** `npm run electron:dev:fresh` script kills a stale Electron process before relaunching. Window title is `Shoresh [DEV]` when `!app.isPackaged`. Dev and packaged databases remain separate.

**Test suite stability (T44/T70/T44-closure, 2026-08-08).** `scripts/ensure-abi.js` now probes the actual compiled binary before trusting the `.abi-target` marker (fixes the root cause of phantom 47-failure cascades when the binary vanished under concurrent sessions). `sleepBecauseTimeIsUnderTest` arrival-waits in `syncClient.test.js` converted to `waitFor`; wall-clock upper-bound assertions removed; `noBareSleeps` guard requires a tagged comment at every marker site. Five consecutive clean full-suite runs recorded on 2026-08-08 (113 files, 1784 tests).

**Engine purity (T69, 2026-08-08).** Remaining JSON-string tolerance deleted from `buildSchedule.js` and `weekCatalog.js`. Array-only contract enforced at every engine input; normalization belongs at the IPC read boundary (`useScheduleData.js:117–122`). DEV-only `assertIdListShape` (T71) makes a violation loud rather than a silent spinner.

**Reconciliation / onboarding program (S0–S5b, merged 2026-08-08, `884659b`).** A second ingest path for reconciling a prior-year import against the camp's live data, replacing blind create-or-skip with field-level recognition and conflict resolution. New screens: `ImportScreen.jsx` (drives the flow), `ReconciliationLedger.jsx` (New/Updated/Unchanged/Clear/Conflict preview, rendered inside `ImportScreen`), `ReadinessHub.jsx` (S5, now the default landing screen — six-state readiness model layered additively over `getSetupGaps`). Slices: **S0** (`ReconciliationPlan` as the shared commit input, ADR `docs/adr/2026-08-08-reconciliation-plan-as-commit-input.md`); **S1a** (import recognizes existing entities via hold-the-whole-on-conflict, `156466c`) — shipped, **S1b** (source aliases/identity resolution) later shipped host-local — see the 2026-08-09 entry below; **S2a** (field provenance — the import-vs-hand-edit bit, schema v29 `operations.source`); **S2b** (field-level update with hand-edit protection/stale-conflict detection); **S2c** (activity-rule and group field updates, `7f0c478`); **S4a/S4b** (enrichment workbook export + re-import round-trip, `048630e`/`2c26efc`), with a shared CSV/formula-injection sanitizer (`src/utils/exportSanitize.js`); **S5a** (Readiness hub screen, `83176dc`); **S5b** (reconciliation-preview ledger, closes T75, `89e708a`). Tickets closed alongside: **T72** (fixed-event/anchor re-import idempotency, `e5af7d2`), **T73** (held-conflict resolution wired into the import UI, `acddd3b`), **T74** (dev-mock ingest fidelity so the flow is exercisable at `localhost:5200`, `a5af872`).

**Reconciliation program closed + ingest quality fixes (2026-08-09).** The remaining slices and the follow-on correctness work all landed on `main`:
- **S1b — host-local source aliases** (`cae6810`) + **"Remember this" UI** (`0cc1be1`): durable alias memory so a confirmed label→entity mapping persists across imports (schema v30, host-local, never synced; single writer `confirmAlias.js`; `confirmed_alias` matching tier). The renderer chain (preload bridge + `localClient` real/mock wrappers + a default-checked checkbox on the ambiguous-identity card, firing best-effort after a successful commit) was the piece the S1b backend had been waiting on. See Key Architectural Decisions.
- **Fixed-event routing + reviewable units** (`d6e5fb9`, ADR `docs/adr/2026-08-09-ingest-fixed-event-routing-and-reviewable-units.md`, revised after a Red Hat pre-code pass caught three silent-data-loss traps): fixed events stop double-writing to the activity catalog (visible/reversible in the review, no engine/golden change); units (tiers) now actually populate — the parser bug that produced zero units for day-column layouts is fixed, plus an editable per-group unit column in the import review with proper set/new/clear states. Inference is conservative (a blank unit, caught by the review column, beats a wrong guess).
- **Re-import provenance protection completed** (`d6e5fb9` for units/groups, **PR #28** `412c5e0` for activity rules): a director's hand-assigned unit, group field, or activity rule is stamped human provenance so a later re-import holds a stale conflict instead of silently overwriting it — all via the one shared `humanEditedFields`/`_humanFields` channel (see Key Architectural Decisions).
- **Drift-guard** (`0f14f3e`): a test pins the renderer's copy of the cohort-scoped entity set (`ALIAS_COHORT_SCOPED`, `src/screens/importAliasScope.js`) equal to the engine's `COHORT_SCOPED`.

---

## Known Issues and Open Items

- **RESOLVED 2026-07-26 (Schedule generation end-to-end — three bugs).** "Generate Schedule" button appeared to do nothing (button stayed stuck on "Generating…" indefinitely). Three root causes found and fixed in commits `0ca1198` / `a03b1ee`:
  1. **`buildSchedule.js` — JSON-string arrays** (`src/engine/buildSchedule.js`): `eligible_tier_ids` and `eligible_group_ids` arrive from SQLite as JSON strings (e.g. `"[]"`), not parsed arrays. The engine compared string `.length === 2` against 0, so the "eligible for all groups" branch was never reached and every activity ended up with 0 eligible groups → 0 slots placed. Fixed by adding a `parseIds()` helper that JSON-parses string values before any eligibility check.
  2. **`projections.js` — `schedule_templates` missing from `PROJECTIONS`** (`electron/ops/projections.js`): `applyProjection` silently no-ops for unregistered entities. Template ops were appended to the op-log but the `schedule_templates` row never materialized — each `generate()` call found 0 rows, created a new UUID, orphaned all prior slots (120 orphaned `template_slots` accumulated across 6 template IDs before discovery). Fixed by adding the `schedule_templates` entry following the same pattern as every other entity.
  3. **`useDeviceMode.js` — `chooseHost()` didn't trigger `init()` re-run** (`src/hooks/useDeviceMode.js`): `init()` is gated on `initNonce` changes. `chooseHost()` set localStorage and React state but never incremented `initNonce`, so when the app started with no persisted mode, `chooseMode` IPC was never called, `syncClient` stayed `null`, and every subsequent `write` IPC threw "sync not initialized". Fixed by adding `setInitNonce((n) => n + 1)` to `chooseHost()`.
- **RESOLVED 2026-07-26 (Camp Setup local-dev blocker + DaysScreen wiring + sidebar grouping).** The reported "unsatisfiable Cohorts prerequisite with no reachable UI path" was a mis-diagnosis: `cohorts` is reachable in the sidebar as "Programs" (commit `68bc36d`) and `ensureCohort` auto-creates a "Main" cohort. A navigation audit (read-through + live walk at localhost:5200) found the real root cause was the **write-blind dev mock**: `src/localClient.mock.js`'s `write()` returned `{status:'applied'}` without persisting, and `list()` read a store `write` never populated — so in the only UI-testable environment (`npm run dev`, no Electron/`window.shoresh`) every create was a silent no-op and no entity (Program/Unit/Group/Time Block/Activity/Anchor) could ever be built. Fixes: (1) the mock now persists field-level writes, `bulkReplace`, and `__deleted__` deletes, and emulates the real `UNIQUE(...)` indexes so it faithfully reproduces the app's collision paths; (2) `DaysScreen.jsx` wired into `App.jsx` `SCREENS` + `Sidebar` (key `days`); (3) an `App.jsx` ref-guard makes the AppShell bootstrap effect (`seedDays`/`ensureCohort`) run once per camp, neutralizing React StrictMode's dev-mode double-invocation (which otherwise double-seeded Mon–Fri → 10 days, since `days_of_operation` has no UNIQUE constraint); (4) sidebar regrouped into labelled "Setup"/"Operations" sections with Conflicts moved to Operations; (5) friendlier CampSetup load-error banner with an inline Retry. Verified end-to-end live at localhost:5200: full Camp Setup → real Schedule grid. Note: all mock changes are dev-only (`window.shoresh` bypasses the mock entirely in Electron).
- **`days_of_operation` has no UNIQUE constraint** — `seedDays.js`'s repair logic works around this, but `electron/db/schema.sql` and `electron/db/localDb.js` have stale comments incorrectly claiming a UNIQUE constraint exists on this table. Low-severity documentation inconsistency, not yet fixed.
- **`sleepBecauseTimeIsUnderTest` guard covers `electron/sync/` only** — bare `setTimeout` sleeps remain unguarded in `electron/main.test.js`, `ScheduleScreen.test.jsx`, `useScheduleData.test.js`, and `ensureCohort.race.test.js`. Tracked in T70's follow-up scope; not a current failure source.
- **Two pre-existing flaky tests** under full-suite parallel load: `electron/sync/discovery.test.js` ("finds an advertised host on the LAN" — passes reliably in isolation) and `electron/sync/syncServer.test.js` ("throttles a burst of rapid login messages" — timing-sensitive under CPU contention). Neither is related to any recent change; both are out of scope until they become reliably reproducible.
- **`import.meta.env.DEV` in the pure engine** (T71) — **RESOLVED 2026-09-01.** The three `buildSchedule.js` sites + one `weekCatalog.js` site now guard with `import.meta.env?.DEV`, so a plain-Node caller (e.g. the MCP server's `schedule_state`/`export_schedule`, which really did crash on this before the fix) skips the DEV-only `assertIdListShape` exactly as a production Vite build does, instead of throwing. Behaviour under Vite/Vitest is unchanged.
- **RESOLVED 2026-09-04** (`src/screens/ImportScreen.test.jsx` frequency-formula mismatch). The test's expectation was stale, not the code: `inferActivityRules` stopped dividing weekly frequency by day count in commit `bd40e7c` (appearances ÷ matched groups only), and the unit test covering that function was updated to match, but this integration test's expectation and comment still assumed the old per-day-divided math — masking `npm run verify` with a stale failure rather than a real one. Fixed in commit `8c248e1`. A full suite run on 2026-09-04 (317 files, 4,641 tests passed, 2 skipped) confirms it is clean; see Test Coverage above.
- **No TLS on the sync connection** (`ws://`, not `wss://`) — explicitly accepted under the trusted-LAN threat model. See `SECURITY.md`.
- **ESLint's Supabase-import ban covers ES `import` only**, not CommonJS `require()` — narrow theoretical bypass in an `electron/**` file. The codebase is ESM-first; `require()` is currently confined to `electron/preload.js`. Low priority.
- **`.env` (gitignored)** contains a local Supabase demo JWT left over from the pre-rebuild setup. Not read by any active code; documented in `.env.example` as legacy/dead.

---

## Removed / Replaced

- **`template_overlays` and `schedule_snapshots.overlays`** — dropped at schema v53 (ADR `docs/adr/2026-08-30-retire-overlay-stamp-subsystem.md`). No live writer had existed since the day-overrides re-point (T108) retired the old field-trip-overlay authoring path; the events/electives/day_overrides family fully covers "special things on the schedule" now. `template_slots` is the only remaining `bulk_replace`-registered entity. A rollback (`v53_down.js`) can recreate the table empty.
- **The old `roots` screen** (`ReconciliationScreen.jsx` mounted with a fixed `mode="inspect"` prop, and the census/RootMap/`RootsBanner` machinery it carried on that route) — replaced by the distinct `RootsHomeScreen.jsx` (ADR `docs/adr/2026-08-28-roots-home-is-a-distinct-screen.md`, WS4). `ReconciliationScreen`'s census/RootMap surface still exists, but only inside the import-a-file reconcile flow. As a side effect of this change, the **post-import grace-window undo banner** (`postImportBanner.jsx`, `useGraceWindowUndo.js`) that used to appear on Roots after a finished import is no longer wired into any live screen (`App.jsx` no longer carries a `justImported` outcome) — see the Home Screen Layout section's Read-only invariant note.
- **`EventScreen.jsx` and `SpecialDaysScreen.jsx`'s standalone authoring rows** — merged into one `SpecialEventsScreen.jsx` (`specialevents` nav key, "Special Events") per ADR `docs/adr/2026-08-29-unify-special-events-screen.md` (PR #235, inline-add capstone PR #242). Their per-entity sub-schedule editors (`EventGridEditor`/`EventCell`, `SpecialDayGridEditor`/`SpecialDayCell`) carried over unchanged as the merged screen's row-detail views; the Plants-side build surface (`SpecialSchedulesScreen`, `schedule:special`) is untouched.
- **The entire spatial / facility-map layer** (`src/screens/DayMapScreen.jsx`, the Locations Map tab, the drag-to-place Canvas, the Tile-World simulation, `src/data/deriveOccupancy.js`, and all associated map helpers) — **deleted in PR #201 (commit `335e311`)**, a `chore:` removal with no ADR. Built across PRs #182/#183 (Day Map, create-in-place) and #196/#199/#200 (Locations Map / Canvas / Tile World), it was removed wholesale after the owner judged it "didn't fit our infrastructure." `LocationsScreen` reverted to list-only CRUD. The schema columns it introduced (`camp_maps`, and `locations.map_id`/`map_geometry`/`kind`/`grid_x`/`grid_y`) are **intentionally kept dormant** so a future map can rebuild on the same seam; no current code renders them. The replacement is not in this app — a schedule-driven animated map (Phaser, real aerial + top-down pixel assets) is a separate standalone initiative.
- **The Roots always-on domain grid** — replaced in PR #206 (ADR `docs/adr/2026-08-27-roots-hub-tiles-are-interface.md`) by making the four census tiles the primary interface; the grid (a Bento since #204) now renders only inside the Understood tile's drill-down. The banner's "Facility map" button was removed at the same time (it pointed at the deleted spatial layer).
- **The manual Sort Order input** — retired in PR #205; sort order is now derived rather than hand-entered.
- **Per-process random session-signing secret** (`const sessionSecret = randomBytes(32)` at module load in `electron/auth/localAuth.js`) — replaced by the per-camp `signing_secret` stored in the `camps` table and looked up per-call. The old approach made cross-process token verification structurally impossible (a Client's own process had a different secret than the Host that issued its token).
- **Login gated purely on local data** — the original design required a Client to already have local `users`/`camps` rows to log in at all, which made first-ever login on a fresh device impossible (circular dependency: no token without login, no login without a prior sync, no prior sync without a token). Replaced by the unauthenticated WebSocket `login` message path described under Auth above.
- **`ReadinessHub` screen and its model** (`src/screens/ReadinessHub.jsx`, its test, `src/screens/readinessHubModel.js`) — deleted in the Roots-as-dashboard arc (PR #113, plan T3). The six-state readiness verdict it rendered now lives on the Roots banner via the single-source `getReadiness`/`describeReadiness` in `src/engine/readiness.js`; the `readiness` navigation key is redirected to `roots`. `src/screens/importOutcomeModel.js` was also deleted (orphaned once the post-import receipt moved onto Roots).
- **`@supabase/supabase-js` dependency, `src/supabase.js`, `supabase/migrations/`** (removed/moved 2026-07-24) — the entire pre-rebuild Supabase backend. Moved to `legacy/supabase/` for historical reference; no longer a project dependency; reintroduction under `src/`/`electron/` is blocked by an ESLint rule. This closes out the renderer-migration-plus-hardening project that spanned Sub-plans A-E plus this final Phase 1 cleanup — every screen, the last bootstrap call site (`seedDays`), and the legacy files themselves are now off Supabase, and the sync layer's one known CRITICAL defect (`bulk_replace` cross-device seq bug) is fixed.
